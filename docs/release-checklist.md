# Production readiness checklist

Every item below is a **release blocker** until checked with evidence (CI run ID, runbook sign-off, security review artifact, or staging test report). Developer validation mode (local JSON store, header auth) does **not** satisfy these gates.

**Do not mark a `PROGRESS.md` task `[x]`** when the row for that area remains unchecked here.

The enterprise gap backlog in [`product/08-enterprise-production-gap-backlog.md`](product/08-enterprise-production-gap-backlog.md) is also release-governing. P0 items there must be implemented or explicitly deferred with owner and sign-off before a production readiness claim.

**Readiness signals:** npm validators prove structural metadata shape only. `production_ready: true` on staging attestation (or `evidence_attestation_complete` on gap audit) means the profile’s required release-evidence kinds are accepted in inventory — **not** production promotion. External promotion requires closing the checklist rows below with staging/security/SOC/legal/provider execution evidence; gap audit `production_ready: true` additionally requires documented checklist gates to be closed.

## Identity, access, and API hardening

- [~] Enterprise OIDC/SSO (or approved IdP) with role mapping to AstraNull RBAC — built-in `oidc-jwt` human auth (RS256 compact JWT + JWKS verification; production default; `dev-headers` and `signed-session` refused at startup). Requires `ASTRANULL_OIDC_ISSUER`, `ASTRANULL_OIDC_AUDIENCE`, `ASTRANULL_OIDC_JWKS_URL` (**HTTPS** when `NODE_ENV=production`); optional `ASTRANULL_OIDC_TENANT_CLAIM` / `ASTRANULL_OIDC_ROLE_CLAIM` / `ASTRANULL_OIDC_USER_CLAIM` / `ASTRANULL_OIDC_JWKS_CACHE_TTL_MS` / `ASTRANULL_OIDC_JWKS_FETCH_TIMEOUT_MS` / `ASTRANULL_OIDC_REQUIRE_MFA` / `ASTRANULL_OIDC_MFA_CLAIM` / `ASTRANULL_OIDC_MFA_VALUES` (bounded JWKS fetch; **redirects not followed**; MFA claim enforcement defaults on in production). Offline prod-auth evidence: `scripts/oidc-prod-auth-preflight.mjs` / `npm run oidc:prod:preflight` writes metadata-only checks for OIDC posture, dev-auth refusal, HTTPS JWKS, and MFA enforcement without contacting an IdP. **Remaining:** real IdP tenant/role mapping, staging login flow, header-only negative test in prod-like deployment, audit/ops signoff
- [~] MFA and session policies per [`docs/security/01-security-model.md`](security/01-security-model.md) — OIDC JWT verification now enforces accepted MFA evidence by default in production (`amr` values such as `mfa`, `otp`, `webauthn`, `fido`, `fido2`, `phishing_resistant`; configurable claim/value policy). **Remaining:** IdP conditional-access/session lifetime policy, refresh-token posture, staging login evidence, break-glass procedure, audit/ops signoff
- [~] API rate limits and abuse detection at gateway or service layer — service fixed-window limiter on `/v1/*` and `/internal/*` (`ASTRANULL_RATE_LIMIT_*`; production refuses `ASTRANULL_RATE_LIMIT_DISABLED=1`); metadata-only gateway load/abuse evidence validator: `scripts/gateway-load-abuse-evidence.mjs` / `npm run gateway:load-abuse:evidence` (release kind `gateway_load_abuse`). **Remaining:** gateway/WAF deployment, staging load/abuse execution, alert routing, security signoff
- [~] WAF or equivalent edge protection for customer-facing API and UI — provider-neutral baseline contract in `src/contracts/edgeProtectionBaseline.mjs` covers TLS, host allowlists, request/header/body limits, bot and credential-stuffing defenses, managed WAF/equivalent rules, origin shielding, logging/audit routing, health endpoint handling, security headers, and proxy-header spoofing controls; `validateEdgeProtectionEvidence()` accepts metadata-only release evidence and rejects raw headers/bodies/logs/payloads/secrets. Operator evidence CLI: `scripts/edge-protection-evidence.mjs` / `npm run edge:protection:evidence`. **Remaining:** actual gateway/WAF deployment config, staging abuse/load validation, alert/log routing evidence, and security signoff
- [~] Bootstrap and service-account secrets — newly issued bootstrap `ast_v1.…` and service-account `svc_v1.…` embed tenant/record lookup hints; full secrets are salted-hashed and verified; addressed parser rejects malformed tokens; invalid addressed service-account bearer auth audits without secret substrings. `authTokenRepository` + `createPostgresAuthServices` are injected by Postgres startup for migrated token/service-account routes. **Remaining production gates:** vault/KMS storage, human SSO/IdP provisioning for humans, staging evidence.
- [~] Penetration test or third-party security review completed and remediations tracked — `src/contracts/productionReleaseEvidence.mjs` defines the metadata-only `third_party_security_review` evidence contract with reviewer, scope, report URI, findings status, remediation tracker, risk acceptance, review date, and security owner; raw logs, credentials, tokens, connection strings, bodies, and payloads are rejected; accepted evidence can be attached through `/v1/production-release-evidence`. **Remaining:** complete independent review, remediate or explicitly accept findings, attach report/tracker evidence, and obtain security signoff

## Authorization proof and evidence custody

- [~] High-scale authorization proof workflow (SOC-009) — required intake on `POST /v1/high-scale-requests` (including request-level `abort_criteria`); expanded required artifact types; server-computed `authorization_pack_status` gates SOC `approve`; per-type metadata artifact upload helpers in High-Scale UI; Postgres-backed high-scale/SOC routes persist and revalidate the workflow. Metadata-only authorization custody validator: `scripts/authorization-custody-evidence.mjs` / `npm run soc:authorization-custody:evidence` (release kind `authorization_custody`). **Remaining:** customer ownership/control proof in durable store, business/legal approval retention, provider integration evidence, scoped dates/vectors/rates signoff, abort criteria legal review, retained artifacts custody, SOC review before approval in staging
- [~] Evidence chain-of-custody — developer-validation: `src/lib/custody.mjs` (`json-key-sorted-v1` canonicalization, SHA-256 digest manifests, `verifyCustodyManifest`); report/finding JSON exports include `custody`; Markdown/HTML report exports embed a Custody section; `/v1/custody/verify` verifies payload/custody pairs without echoing payloads and audits `custody.verified`; export and verification audits record safe digest metadata only (`format`, `content_sha256`, `custody_schema_version`, verification status); `previous_audit_hash` aligned to audit-chain predecessor; Postgres report/finding exports are wired through `runtime.services.reports` and verification audit uses Postgres audit service when available (`tests/unit/custody.test.mjs`, `tests/unit/audit-chain.test.mjs`, `tests/unit/postgres-service-adapters.test.mjs`, `tests/integration/completion-polish.test.mjs`, `tests/integration/custody-verification-api.test.mjs`). Immutable snapshot manifest validator: `scripts/evidence-snapshot-manifest.mjs` / `npm run evidence:snapshot:manifest`. **Remaining:** durable immutable evidence snapshots, external signing/KMS ceremony, retained export custody storage, retention/legal-hold enforcement evidence, staging signoff
- [~] Compliance report templates complete — SEC-009 now ships executive, technical, SOC, audit, board, SOC 2, ISO 27001, DORA, NIS2, and internal-audit mapping templates in `src/contracts/complianceReports.mjs`; JSON/markdown/HTML report exports include mapping sections covered by custody. Metadata-only compliance/legal signoff validator: `scripts/compliance-legal-signoff-evidence.mjs` / `npm run release:compliance-legal:evidence` (release kind `compliance_legal_signoff`). **Remaining:** auditor/legal review, staging export walkthrough, and immutable evidence retention signoff.

## Data plane (PostgreSQL)

- [~] Schema/RLS contract exists — [`db/schema.sql`](../db/schema.sql), [`db/migrations/0001_core_validation_loop.sql`](../db/migrations/0001_core_validation_loop.sql), [`db/migrations/0002_production_ledgers.sql`](../db/migrations/0002_production_ledgers.sql), [`db/migrations/0003_runtime_shape_parity.sql`](../db/migrations/0003_runtime_shape_parity.sql), [`db/migrations/0004_validation_ledger_indexes.sql`](../db/migrations/0004_validation_ledger_indexes.sql), [`db/migrations/0005_verdict_placement_confidence.sql`](../db/migrations/0005_verdict_placement_confidence.sql), [`db/migrations/0006_notification_rule_triggers.sql`](../db/migrations/0006_notification_rule_triggers.sql), and [`db/migrations/0007_production_release_evidence.sql`](../db/migrations/0007_production_release_evidence.sql) (core validation loop + production ledgers + runtime shape parity + validation-ledger indexes + verdict `placement_confidence_json` + notification trigger/metadata JSON + production release evidence ledger; operators on non-empty DBs must preflight duplicate open findings before `uniq_findings_open_target_check`); verified by `scripts/validate-db-schema.mjs` in CI via `make verify`
- [~] Postgres runtime boundary — tenant-scoped repositories, service adapters, `createPostgresRuntime`, and `src/startup.mjs` now let `ASTRANULL_PERSISTENCE_MODE=postgres` initialize the pool/runtime, inject migrated services, use Postgres health on `/ready`, close cleanly, and fail closed with `postgres_route_not_wired` for missing service injections. Covered by Postgres repository/service/runtime tests, `tests/unit/startup.test.mjs`, and `tests/unit/server-postgres-mode.test.mjs`. **Remaining:** staging/live DB acceptance execution evidence; tenant unit-of-work/concurrency hardening under load. Gated DB-backed runtime wiring smoke: `scripts/postgres-runtime-smoke.mjs` (`ASTRANULL_POSTGRES_RUNTIME_SMOKE=1` + `ASTRANULL_DATABASE_URL`; optional `ASTRANULL_REQUIRE_POSTGRES_RUNTIME_SMOKE=1` in release automation); verifies `createPostgresRuntime` service families and `health()` without customer data or safe-check execution (`tests/unit/postgres-runtime-smoke.test.mjs`). `/v1/state` is migrated via `runtime.services.state` with evidence-backed readiness summaries plus repository-backed high-scale and kill-switch posture. `/v1/test-runs` (start/cancel/finalize) and agent observation orchestration are migrated via `runtime.services.testRuns` with safe-window/rate/cooldown/concurrency/kill-switch/prerequisite gates, signed worker job creation, agent job dispatch, probe/agent correlation, automatic and forced finalization, verdict and finding publication, metadata-only raw-field rejection, and audit logging. `/v1/high-scale-requests` and `/internal/soc/high-scale/*` are migrated via `runtime.services.highScale` with SOC approval gates, metadata-only telemetry, post-test report close gates, dry-run adapter state, kill-switch auto-stop, and state-change notifications. `/v1/production-release-evidence` is migrated via `runtime.services.productionReleaseEvidence` with metadata-only contract validation, redaction, tenant-scoped reads, and safe audit metadata; `/v1/events` is migrated via `runtime.services.events` with idempotent metadata-only ingestion; `/v1/notifications` is migrated via `runtime.services.notifications` with metadata-only delivery ledgers (real outbound providers still gated); `/v1/secrets` is migrated via `runtime.services.secretVault` (KMS/HSM and staging evidence remain gates); report/finding exports are migrated via `runtime.services.reports` (immutable export storage/signoff still gates); `/internal/probe/jobs` and `/internal/probe/jobs/:id/result` are migrated via `runtime.services.probeJobs` with tenant-scoped worker HMAC and metadata-only result ingestion.
- [~] Versioned migrations **applied** in staging/production by a migration runner — local runner and startup preflight exist, and `src/contracts/productionReleaseEvidence.mjs` defines the `migration_apply` evidence contract for environment, DB cluster reference, migration version, runner evidence, timestamps, operator, and post-apply check; accepted evidence can be attached through `/v1/production-release-evidence`. **Remaining:** run migrations in staging/production, attach redacted runner/post-apply evidence, complete rollback/fix-forward note, and obtain DB/operator signoff
- [~] Postgres startup preflight — `scripts/postgres-startup-check.mjs` (`npm run postgres:startup-check`): verify-only requires `ASTRANULL_DATABASE_URL`, bounded pool parse, ping, latest migration applied; `--migrate` applies pending migrations then verifies; URLs redacted; `tests/unit/postgres-startup-check.test.mjs`.
- [~] Runtime Postgres production startup injects migrated service families (catalog, auth, agents, agent updates, validation safe loop, events, notifications, evidence, findings, production release evidence, secrets, reports, state, probe jobs, high-scale/SOC, audit, retention); request-time `postgres_route_not_wired` applies only when a route's required injected service is missing or the handler is not yet Postgres-backed. **Remaining:** staging/live DB acceptance execution evidence, gated runtime smoke in target environments, tenant concurrency under load.
- [~] Tenant isolation — gated staging harness `scripts/postgres-acceptance.mjs` (`npm run postgres:acceptance`, `ASTRANULL_POSTGRES_ACCEPTANCE=1` + `ASTRANULL_DATABASE_URL`) exercises migrations, RLS/composite FK catalog checks, `withTenantContext` isolation, and cross-tenant reject; skipped unless explicitly enabled; not part of default `make verify`. Gated runtime wiring smoke `scripts/postgres-runtime-smoke.mjs` (`ASTRANULL_POSTGRES_RUNTIME_SMOKE=1` + `ASTRANULL_DATABASE_URL`) initializes `createPostgresRuntime`, verifies migrated service families, and calls `runtime.health()` without seeding customer data. Staging concurrency evidence validator: `scripts/postgres-concurrency-evidence.mjs` / `npm run postgres:concurrency:evidence` rejects DB URLs, raw SQL, row payloads, and customer data. Static tenant-query audit: `scripts/postgres-tenant-query-audit.mjs` / `npm run postgres:tenant-query:audit` scans Postgres repositories/adapters for tenant-scoped table access without tenant context and writes metadata-only findings. **Remaining:** concurrent load execution in staging/prod, DB execution evidence, runtime adapter on all tenant queries
- [~] Encryption at rest for token hashes, agent credentials, and sensitive metadata — salted token/`svc_`/`agc_` hashes; newly issued agent credentials `agc_v1.…` embed tenant/agent id lookup hints (full secret still verified against stored salt/hash; legacy opaque `agc_…` dev credentials supported); invalid addressed `agc_v1` bearer attempts audit `agent.auth_denied` only when a matching `(tenant_id, agent_id)` row exists (nonexistent/mismatched hints and unknown legacy route agents return `401` without tenant-local audit); packaged agent HTTPS API default with localhost HTTP opt-in (`--allow-insecure-localhost-api` / `ASTRANULL_ALLOW_INSECURE_LOCALHOST_API=1`); packaged identity `/var/lib/astranull/identity.json`; control-plane redaction and agent logs cover full dotted `agc_v1…` values; integration secrets in AES-256-GCM envelopes via admin metadata API (`/v1/secrets`); secret rotation evidence validator: `scripts/secret-rotation-drill-evidence.mjs` / `npm run secret:rotation:evidence`; **remaining:** KMS/HSM, automated key rotation drill execution, staging/live DB evidence
- [~] Backup, restore, and DR drill documented and executed (`SEC-007`) — developer-validation `scripts/backup-dev-store.mjs` / `scripts/restore-dev-store.mjs`, manifest validation, `tests/unit/dr-backup-restore.test.mjs`, `docs/disaster-recovery.md` and operator runbook (`dr:backup:dev` / `dr:restore:dev`). Production drill evidence validator: `scripts/dr-restore-evidence.mjs` / `npm run dr:restore:evidence` enforces metadata-only RPO/RTO, custody, and signoff artifacts. **Remaining:** runtime Postgres backups, encrypted scheduled backups, production restore/failover drill execution, RPO/RTO signoff
- [~] Data retention and regional residency enforced per tenant `privacy_settings` — tenant metadata purge (`metadata_retention_days`, immediate purge on privacy PATCH, `enforceMetadataRetentionForTenant`); `evidence_retention` normalized with defaults/clamps; `buildRetentionPolicySnapshot` and legal hold block metadata deletion (`privacy.retention_legal_hold` audit); Postgres mode exposes transactional `runtime.services.retention.enforceMetadataRetentionForTenant` and dry-run reporting, preserving audit/high-scale/SOC/finding/test-run governance rows; operator CLI `scripts/postgres-retention-runner.mjs` (`--tenant-id` / `--tenant-ids-file`, `--dry-run`, `--out`) delegates deletes to the retention service and is scheduled externally (not an in-repo daemon); `tests/unit/postgres-retention-runner.test.mjs`. **Remaining:** staging scheduled run evidence (external cron/CronJob + retained JSON summaries), regional residency, vault/KMS, immutable legal archive/export signoff, governance deletion enforcement

## Secrets and agent supply chain

- [~] Secrets in KMS/HSM or approved vault — never plaintext in logs or repos — dev envelopes + redacted `/v1/secrets` API and RBAC landed; service-account rotate route landed for dev store; metadata-only KMS/vault posture validator: `scripts/kms-vault-posture-evidence.mjs` / `npm run kms:vault:evidence` (release kind `kms_vault_posture`). **Remaining:** external KMS/HSM deployment, envelope rotation policy, key-rotation drill execution, staging/live evidence, security signoff
- [~] Agent packages — generic Linux tarball builder (`scripts/package-agent.mjs`, `agent:package`) produces `.tar.gz`, manifest, and optional Ed25519 `.manifest.json.sig` (`AG-005`). Evidence utilities now cover SBOM/provenance (`scripts/agent-sbom-provenance-evidence.mjs` / `npm run agent:sbom:provenance:evidence`) and install/uninstall matrix summaries (`scripts/agent-install-matrix-evidence.mjs` / `npm run agent:install:matrix:evidence`). **Remaining:** deb/rpm/container signing (GPG/cosign), customer CDN/mirror custody and operational runbooks for published artifacts, installer-side signature enforcement bound to tenant trust keys on hosts, real distro/Kubernetes validation matrix (`AG-002`)
- [~] Agent update channel — control-plane `POST/GET /v1/agent-updates` with required `distribution: { manifest_url, signature_url, artifact_url }` (absolute HTTPS; URL credentials rejected; `artifact_url` basename must match signed manifest artifact name; query strings allowed; audit metadata excludes distribution URLs), rollback route, `POST/GET /v1/agent-update-trust-keys` and revoke, active trusted signing-key enforcement (`untrusted_signing_key`), shared signed-manifest verification, staged rollout ledgers, agent poll returns `download` on `GET /v1/agents/:id/update`, agent poll/status routes, and Postgres-mode persistence through `agentUpdateRepository` + `runtime.services.agentUpdates`; agent CLI `--verify-update-manifest`, host-side `--apply-update-manifest`, and `--download-and-apply-update` are available (HTTPS-only URLs by default; localhost HTTP only with `--allow-insecure-localhost-downloads`; rejects URL credentials; bounded download size/timeout; HTTP redirects not followed; manifest/signature/artifact verification before apply; installs to `<installRoot>/releases/<version>/`, writes `<installRoot>/current.json`) (`AG-014`); metadata-only trust-key ceremony/rotation/revocation drill validator (`scripts/agent-trust-key-ceremony-evidence.mjs` / `npm run agent:trust-key:evidence`; production release kind `agent_trust_key_ceremony`; `tests/unit/agent-trust-key-ceremony-evidence.test.mjs`). **Remaining:** hosted artifact storage custody/CDN/mirror operational runbooks and staging evidence, unattended daemon orchestration and service restart, live staging trust-key drill execution and installer/update-daemon enforcement, deb/rpm/container rollout integration, staging distro/Kubernetes rollout + rollback drill, IdP/MFA for release admins
- [~] Agent mTLS or equivalent strong identity for control channel in production — `ASTRANULL_AGENT_IDENTITY_MODE` defaults to `gateway-mtls` in production, refuses bearer-only mode, and requires the forwarded client certificate fingerprint to match the registered agent fingerprint on agent heartbeat/jobs/observations/update routes; dev/test defaults to bearer for local validation. Metadata-only release evidence validator: `scripts/agent-mtls-gateway-evidence.mjs` / `npm run agent:mtls:evidence` (production release kind `agent_mtls_gateway`). **Remaining:** live production gateway/mTLS proxy deployment, PKI issuance/rotation/revocation drill execution in staging, staging agent registration + heartbeat proof capture, and security signoff on spoofing controls.

## Safe probes and detection

- [~] External probe workers consume **signed** jobs only (not in-process simulation as the sole path) — signed payloads include bounded `probe_profile` metadata; tampering `probe_profile` or target `port` invalidates signatures; jobs preserve target `port`/`protocol`; worker sends bounded metadata probes (HTTP HEAD, DNS lookup only, TCP one connect, metadata-marker no network); Postgres worker routes require and sign `x-probe-tenant-id`. Unit/integration/e2e and safety-scan coverage in CI. Fleet matrix evidence validator: `scripts/probe-fleet-matrix-evidence.mjs` / `npm run probe:fleet:matrix:evidence`. **Remaining:** production fleet deployment, staging E2E, and signed-worker fleet matrix execution (simulation may remain default in dev/CI)
- [~] Rate caps, scheduling, and stop controls enforced per [`docs/security/02-safe-testing-policy.md`](security/02-safe-testing-policy.md) — customer-runnable catalog entries define bounded `probe_profile`, safe constraints, evidence requirements, and stop conditions; `tests/unit/vectors.test.mjs`, signed-job tests, worker safety attestation, safe-window/rate/cooldown/concurrency gates, and kill-switch tests run in `make verify`. **Remaining:** staging signed-worker fleet matrix, live target acceptance, and production gateway/WAF abuse evidence.
- [~] Safe check library acceptance tests per [`docs/detection/12-vector-test-matrix.md`](detection/12-vector-test-matrix.md) — every customer-runnable safe check declares `supported_targets`, `required_customer_setup`, `evidence_required`, `verdict_logic`, and bounded `probe_profile`; vector, signed-job, worker-metadata, port-propagation, and safety-scan tests in CI. Vector safety evidence validator: `scripts/vector-safety-policy-evidence.mjs` / `npm run vector:safety:evidence`. **Remaining:** full staging signed-worker fleet matrix and live-target acceptance per vector row
- [~] Agent metadata observation (evidence-gated uploads) — Linux agent uploads only when local signal matches; `--packet-metadata-file` and `--mirror-metadata-file` metadata-only JSONL inputs; raw/payload/header/log rejected at ingest; backend placement diagnostics (`proven`, `needs_baseline`, `missing_agent`, `misplaced_risk`) feed readiness scoring (`DET-013`). **Remaining:** signed agent packages with installer enforcement, UI surfacing for placement, customer TAP/mirror staging validation, and per-verdict agent confidence scoring (`High` / `Medium` / `Low` / `Invalid`)
- [~] Evidence exports redacted; audit trail immutable for auth, tokens, tests, SOC actions — tamper-evident audit hash chain and redacted exports validated in dev (`BE-015`); Postgres `auditRepository.appendAuditEvent` and `listAuditEntries` are exposed through `runtime.services.audit` for RBAC denials, report/finding export custody, and `GET /v1/audit-log`. **Remaining:** immutable production audit storage, legal evidence export signoff, and staging audit review

## SOC and high-scale governance

- [~] Customer high-scale request intake (SOC-009 partial) — `POST /v1/high-scale-requests` requires production-shaped payload including SOC-009 fields (`environment`, `business_criticality`, `requested_scenario_families`, `requested_limits`, `stop_criteria`, `abort_criteria`, plus objective/reason, normalized `requested_window`, `emergency_contacts`, `provider_context`, `scope_confirmation`); intake validated and stored on request record in dev and Postgres modes; High-Scale UI form submits metadata-only contract; SOC execution remains gated. **Remaining:** full staged customer workflow, durable document store, legal/offline signoff, browser accessibility matrix
- [~] Provider approval checklist — `provider_approval_checklist` on high-scale create; `provider_approval` artifacts sync checklist status; expired valid windows block approve; compatibility path for requests without checklist. Operator evidence validator: `scripts/provider-approval-evidence.mjs` / `npm run soc:provider-approval:evidence`. **Remaining:** live provider integration, staging approval evidence execution, legal export/signoff
- [~] Customer authorization templates and legal review for high-scale tests — `src/contracts/authorizationTemplates.mjs` and [`docs/templates/high-scale-authorization-pack.md`](templates/high-scale-authorization-pack.md) cover every required high-scale artifact type and enforced proof field, including business approval, legal approval, scope/rate plan, abort criteria, and provider approval. **Remaining:** customer/legal review, durable document custody, retained signed artifacts, export retention/signoff, and staging workflow walkthrough.
- [~] Provider approval workflow (CDN/cloud/on-prem) with evidence retention — SOC-010 provider-path metadata catalog and high-scale checklist gates now distinguish AWS, Azure, GCP, CDN/WAF, carrier, on-prem, and partner-lab paths with required evidence, limits, accepted paths, and stop controls. **Remaining:** live provider integrations, durable document custody, staging approval evidence, and legal retention/export signoff
- [~] SOC execution adapter connected to **governed** partner or internal systems — `src/contracts/governedExecutionAdapter.mjs` defines production adapter registration metadata, allowed adapter types, required capabilities (`validate_scope_hash`, `start_with_soc_token`, `stop_or_abort`, `status`, `metrics_metadata`, `evidence_export`, `audit_events`, `kill_switch_stop_path`), and metadata-only validation that rejects secrets/raw commands/payloads/traffic-generator fields. Operator evidence validator: `scripts/governed-adapter-evidence.mjs` / `npm run soc:adapter:evidence`. **Remaining:** actual partner/internal adapter connection, signed SOC execution-token issuance, staging start/stop/kill-switch proof, telemetry/evidence export proof, and security/SOC signoff; dry-run adapter alone is not sufficient
- [~] Kill switch validated end-to-end with probe fleet and adapter stop paths — Postgres `runtime.services.highScale.setKillSwitch` auto-stops governed high-scale runs, auto-cancels active safe test runs, and cancels open signed-worker probe jobs for those runs (`probeJobsRepo.cancelOpenProbeJobsForTestRuns`); per-job metadata-only audit `probe_job.kill_switch_auto_cancel`; activation response and `soc.kill_switch.activated` audit include `cancelled_probe_job_ids` with `stopped_request_ids` and `cancelled_run_ids` (`tests/unit/postgres-high-scale-service-adapters.test.mjs`). `src/contracts/killSwitchValidation.mjs` defines metadata-only exercise evidence for tenant kill-switch activation, new safe-run blocking, active safe-run cancellation, probe-fleet lease stop, adapter stop invocation, audit timeline capture, and guarded resume; raw headers/bodies/packet payloads/logs/secrets are rejected. Operator drill evidence validator: `scripts/kill-switch-drill-evidence.mjs` / `npm run soc:kill-switch:evidence`. **Remaining:** execute the drill in staging with the signed probe fleet and governed adapter, prove external workers halt in flight (DB cancellation alone is insufficient), attach stop-path evidence, verify alerting/notification routing, and complete SOC/security signoff
- [~] High-scale metadata telemetry — `highScaleTelemetry` store; SOC-only `POST/GET /internal/soc/high-scale/:id/telemetry`; metadata-only categories/statuses; raw fields rejected; `high_scale.telemetry_recorded` audit; post-test reports include safe `telemetry_summary` (`DET-012`). **Remaining:** live provider/staging telemetry feeds and automated adapter ingestion
- [~] SOC Console developer-validation controls — `#soc` route exposes queue, artifact review, schedule, dry-run start/stop, adapter status, SOC note, post-test report (with `telemetry_summary` when recorded), close, kill switch, and action output; SOC telemetry endpoint available for manual metadata entry; backend RBAC/gates remain authoritative; no production/live adapter or provider telemetry implied. **Remaining:** enterprise IdP workflow, live adapter/provider telemetry, governed execution signoff
- [~] Post-test report and close gate — backend `POST/GET /internal/soc/high-scale/:id/post-test-report` (SOC-only); close requires stopped request and stored report (`post_test_report_required`); metadata-only derived timeline/artifacts with redaction; developer-validation SOC UI can file/view report and close when gated. **Remaining:** durable legal evidence retention, full production SOC workflow, staging signoff
- [~] No customer API path can start unmanaged traffic; SOC role and approval state machine enforced — high-scale start is only on `/internal/soc/high-scale/:id/start`, guarded by `soc:high_scale`, authorization pack, provider checklist, dual approval/scope hash, schedule window, kill switch, and adapter state tests; production config refuses `ASTRANULL_HIGH_SCALE_ADAPTER_MODE=dry-run`. **Remaining:** governed partner/internal adapter connection, staging SOC exercise, and provider stop-path evidence.

## Observability, support, and documentation

- [~] Metrics, alerting, and SLO dashboards per [`docs/backend/08-observability.md`](backend/08-observability.md) — `/metrics`, `/v1/observability`, core counters, and minimum alert/dashboard contract are documented; integration smoke verifies observability endpoints. Operator evidence validator: `scripts/observability-slo-evidence.mjs` / `npm run observability:slo:evidence` requires authenticated scrape metadata, dashboard IDs, alert routes, SLO targets, incident drill, on-call owner, and redaction policy. **Remaining:** authenticated production scraping, alert routing, dashboards, staging incident drill execution, and SLO signoff.
- [~] On-call runbook and [`docs/support-playbook.md`](support-playbook.md) staffed with SLAs — severity table, SOC/high-scale safety handling, and developer troubleshooting exist. Support readiness evidence validator: `scripts/support-readiness-evidence.mjs` / `npm run support:readiness:evidence` validates named rotation, SLA policy, escalation contacts, incident tabletop, customer comms templates, and support signoff. Settings UI exposes a separate **Support & on-call readiness** panel (reference-only SLA/escalation/SOC state; does not imply staffed production support). **Remaining:** named rotation, customer SLA policy, escalation contacts, and rehearsal execution evidence.
- [~] Incident playbooks, staging evidence, backup/restore drills, and production support SLAs complete — support playbook and developer DR scripts/docs exist. **Remaining:** production Postgres restore/failover drill, incident tabletop, provider kill-switch drill, and signed support SLA approval.
- [~] Verdict UX includes a "Why this verdict?" panel with external probe evidence, internal agent evidence, placement confidence, conclusion, and remediation link — developer-validation Test Runs panel landed (`renderVerdictExplanation`, `tests/e2e/ui-smoke.test.mjs`); backend `verdict.placement_confidence` (DET-014) in UI and JSON report export. Browser/accessibility evidence validator: `scripts/ui-accessibility-matrix-evidence.mjs` / `npm run ui:accessibility:matrix:evidence`; placement confidence staging matrix validator: `scripts/placement-confidence-staging-evidence.mjs` / `npm run placement:staging:evidence` (release kind `placement_confidence_staging`). **Remaining:** actual browser/accessibility matrix execution, findings detail parity, staging/live placement mode matrix execution, staging/live DB evidence for verdict/report export paths (`0005_verdict_placement_confidence` schema/repository parity landed)
- [~] Production release evidence visibility in UI — developer-validation `#release-evidence` page and compact Settings panel (`renderReleaseEvidencePanel` in `apps/web/ui-helpers.js`) list accepted kinds, validation summary, release id, created time, and custody URI preview from `GET /v1/production-release-evidence` without raw bodies; gate copy states metadata does not mean production readiness is complete. **Remaining:** browser/accessibility matrix execution, staging operator review workflow, and legal/security signoff that UI matches release governance
- [~] [`docs/api.md`](api.md) matches deployed routes and production auth model — `oidc-jwt` env contract and service-account independence documented; persistence model and staging IdP signoff still pending
- [~] [`docs/operator-local-runbook.md`](operator-local-runbook.md) production section exercised in staging — `src/contracts/productionReleaseEvidence.mjs` defines the `operator_runbook_exercise` evidence contract for environment, runbook version, exercise window, operator, evidence URI, exceptions, and signoff reference; `scripts/release-evidence-bundle.mjs` / `npm run release:evidence:bundle` validates metadata-only evidence bundles before API submission; `scripts/staging-readiness-attestation.mjs` / `npm run release:staging-attestation` aggregates required production release evidence kinds into a metadata-only attestation where `production_ready: true` means profile inventory is complete (present, contract-valid, accepted kinds only — not production promotion); optional/future kinds are documented separately and do not satisfy the gate alone; accepted evidence can be attached through `/v1/production-release-evidence`. **Remaining:** execute the production section in staging, attach exception log and signoff, link evidence to the release review, and pass staging attestation with a complete accepted evidence set
- [~] `make verify` green on release branch (lint, unit/integration/e2e tests, `scripts/safety-check.mjs`, and `scripts/postgres-tenant-query-audit.mjs` via Node — see [`.github/workflows/ci.yml`](../.github/workflows/ci.yml)); latest local pass recorded on 2026-07-02: `make NODE=/opt/homebrew/bin/node verify` (lint, 1143 unit, 82 integration, 2 e2e, safety-check, tenant-query audit 0 findings). Staging E2E matrix evidence validator: `scripts/staging-e2e-matrix-evidence.mjs` / `npm run release:staging-e2e:evidence` (release kind `staging_e2e_matrix`). **Remaining:** execute SSO login, signed agent, probes, and report flows in staging and attach operator-attested matrix evidence (local validators alone are insufficient; green local verify is not production promotion).
- [~] Control-plane container image built from root [`Dockerfile`](../Dockerfile), scanned, and promoted with recorded digest — `scripts/container-release-evidence.mjs` / `npm run container:evidence` builds from the root Dockerfile, inspects image ID/repo digests, optionally runs `trivy` or `grype`, and writes safe release metadata for `/v1/production-release-evidence` kind **`control_plane_container_release`** without secrets; unit tests cover the evidence contract. **Remaining:** actual release CI build, scanner output, registry promotion digest, image signing/provenance, runtime secrets, Postgres acceptance, and staging signoff.
- [~] Production readiness gap audit and rehearsal samples — `scripts/production-readiness-gap-audit.mjs` / `npm run release:gap-audit` summarizes required evidence kinds, attestation posture, and open checklist gates (metadata-only; does not close external blockers) and **exits nonzero** when `production_ready=false`. Strict release-evidence kinds (including `governed_adapter` and kill-switch drill contracts) report `invalid_fields` on failed validation. `scripts/generate-release-evidence-samples.mjs` / `npm run release:sample-evidence` writes `rehearsal_only` fixture bundles for operator walkthroughs only — **not** staging signoff and must not be submitted as production evidence without replacement. **Remaining:** complete accepted evidence set from real staging/security/SOC/legal/provider/KMS operations.
- [~] `PROGRESS.md` has no `[x]` on production-only tasks without evidence for the rows above — this checklist now calls out release blockers for local-only `[~]` rows. **Remaining:** final release review before branch cut to downgrade any unsupported `[x]` rows or attach staging/legal evidence.
- [~] Trademark, domain, and compliance sign-off — compliance mapping implementation is tracked above. **Remaining:** trademark/domain review, customer auditor review of mapping accuracy, legal sign-off for DORA/NIS2/board packs, and staging export walkthrough.

## External integrations (when enabled)

- [~] Notifications (email/Slack/Teams/webhook) behind explicit customer configuration — validated rules, per-event delivery-attempt ledger, redacted metadata, audits, HTTP 400 on invalid rules; no silent outbound send in dev; metadata-only retry-plan worker `scripts/notification-retry-worker.mjs` / `npm run notification:retry:worker`; Postgres retry runtime runner `scripts/notification-retry-runner.mjs` / `npm run notification:retry:runner` (externally scheduled, default metadata-only); provider configuration evidence validator `scripts/notification-provider-config-evidence.mjs` / `npm run notification:provider:evidence`. **Remaining:** real email/Slack/Teams adapters, always-on persistent retry scheduler, per-tenant encrypted credentials, and staging delivery evidence
- [~] Optional cloud/CDN connectors remain optional; core product does not require customer cloud credentials — product-scope tests scan implementation paths for default cloud SDK/credential patterns, UI/API copy states no cloud credentials, and SOC-010 provider paths are metadata-only approval checklists. **Remaining:** future connector feature flags and security review before enabling any optional provider integration.

## Rollback

- [~] DB migration down scripts or forward-fix plan documented per release — operator runbook now requires migration forward-fix/down planning and `postgres:startup-check`/acceptance evidence before promotion; metadata-only rollback/fix-forward plan validator: `scripts/rollback-fixforward-evidence.mjs` / `npm run rollback:evidence` (tested command references, backup reference, adapter/probe flag plans, comms, success criteria, signoffs; rejects raw scripts/secrets/dumps); production release evidence kind `rollback_fixforward` in `src/contracts/productionReleaseEvidence.mjs`. **Remaining:** per-release migration note, rollback owner signoff, and staging migration drill execution.
- [~] Feature flags or config toggles for high-scale and probe workers tested — `ASTRANULL_PROBE_MODE` defaults to `signed-worker` in production, requires a 32+ char worker secret, and refuses explicit `simulation` when `NODE_ENV=production` (simulation remains valid for developer validation and CI only); `ASTRANULL_HIGH_SCALE_ADAPTER_MODE` defaults to `governed-adapter`, supports `disabled`, and refuses `dry-run` in production; unit tests cover the contract. **Remaining:** staging run with real worker fleet and governed adapter mode.
- [~] Developer validation rollback: revert git tag and reset local `.data/astranull-dev.json` only — not a production rollback procedure. **Remaining:** execute production rollback/fix-forward tabletop or staging drill, attach validated plan evidence to the release review, and complete operator/DB signoff.
