# AstraNull Progress Tracker

Use this file as the single build tracker. Each task links to the doc that explains how it must be implemented.

Status: `[ ]` not started, `[~]` in progress, `[x]` complete, `[!]` blocked, `[?]` needs decision.

## Production readiness rule

**Do not mark `[x]`** unless the linked implementation doc acceptance criteria are met **and** the applicable gates in [`docs/release-checklist.md`](docs/release-checklist.md) have verification evidence (tests, staging run, security review). Developer validation behavior (local JSON store, header auth, probe simulation, adapter dry-run) does **not** satisfy production completion goals.

When a task is partially implemented for local engineering only, keep `[~]` and state the **production release blocker** in the completion goal.

Metadata-only evidence validators (`npm run …:evidence`, `npm run release:gap-audit`) check JSON contracts and forbidden-field rules only. They do **not** substitute for staging execution, security review, SOC/legal signoff, KMS/vault custody, or provider evidence. `npm run release:sample-evidence` writes rehearsal fixtures (`rehearsal_only: true`) for operator walkthroughs — never treat passing sample generation or local validators as production readiness.

## 0. Product foundation

| Status | ID | Task | Implementation doc | Completion goal |
|---|---|---|---|---|
| [x] | P0-001 | Finalize product promise and scope | [Platform Overview](docs/product/01-platform-overview.md) | Final no-access-first promise in `README.md`, `docs/product/01-platform-overview.md`, `docs/product/02-scope-and-principles.md`, and `apps/web/app.js`; enforced by `tests/unit/product-scope.test.mjs`. |
| [x] | P0-002 | Enforce no-inventory-discovery scope | [Scope & Principles](docs/product/02-scope-and-principles.md), [ADR-001](docs/adr/0001-no-ip-inventory-discovery.md) | No default cloud SDK/credentials and no automatic IP inventory discovery paths; `scripts/safety-check.mjs` + `tests/unit/product-scope.test.mjs` guard scope. |
| [x] | P0-003 | Define user roles and permissions | [Personas & Roles](docs/product/03-personas-and-roles.md) | RBAC roles created for owner, admin, engineer, SOC, auditor, viewer. |
| [x] | P0-004 | Define target group model | [Target Groups](docs/product/04-target-groups.md) | Target groups, targets, expected behavior, ownership, and environment fields exist. |
| [x] | P0-005 | Define readiness score | [Evidence & Scoring](docs/detection/04-evidence-and-scoring.md) | Evidence-backed readiness factors (coverage, agents, findings, freshness, SOC) with unit tests; stale/absence does not inflate score. |
| [x] | P0-006 | Define public entry and internal management boundaries | [Public Landing and Internal Management](docs/product/13-public-landing-and-internal-management.md) | Public landing (`/`), sign-up intake (`/signup`, `POST /v1/signup-requests`), customer portal (`/app`), staff-only internal management (`/internal/admin`, `/internal/admin/*` APIs with staff RBAC), and SOC console boundaries are implemented with dev-json and Postgres service injection; customer principals are denied on staff APIs; route/auth tests in `tests/integration/public-internal-management-api.test.mjs`, `tests/unit/staff-roles.test.mjs`, `tests/unit/server-postgres-mode.test.mjs`. **External:** production IdP staff MFA, separate internal host/edge controls, and staging operator workflow. |

## 1. UX and frontend

| Status | ID | Task | Implementation doc | Completion goal |
|---|---|---|---|---|
| [x] | UX-001 | Build onboarding wizard shell | [Onboarding Flow](docs/flows/01-customer-onboarding.md), [Page Spec](docs/ux/01-pages-and-tabs.md) | Developer-validation wizard (`renderOnboardingWizard` + `onboard-create-env/tg/target/token`, `onboard-start-run`, Linux/Docker/Helm install tabs, copy command, troubleshooting on install step) wired to existing APIs; `tests/unit/ui-helpers.test.mjs`, `tests/e2e/ui-smoke.test.mjs`. **External:** agent heartbeat verification step, placement test, browser/accessibility matrix. |
| [x] | UX-002 | Build dashboard page | [Dashboard UX](docs/ux/02-dashboard.md) | Developer-validation dashboard: readiness gauge/radar/trend, vector heatmap, open findings/agents/tg metrics, placement diagnostics, recent runs + pending high-scale/SOC request lists, no-target-group friendly empty state (`PAGE_EMPTY_STATES.dashboard`); `tests/unit/ui-helpers.test.mjs`, `tests/e2e/ui-smoke.test.mjs`. **External:** priority actions logic, stale-data labeling, browser/accessibility matrix, live-data fidelity. |
| [x] | UX-003 | Build target groups page | [Target Group UX](docs/ux/03-target-groups-page.md) | Developer-validation list/create + tabbed detail panel (`renderTargetGroupDetailPanel` in `ui-helpers.js`): Targets (patch/delete), Recent runs, Settings (name/description/safety policy + archive via DELETE); `tg-detail` selection; friendly empty state; `tests/unit/ui-helpers.test.mjs`, `tests/e2e/ui-smoke.test.mjs`. **External:** checks/findings tabs, browser/accessibility matrix. |
| [x] | UX-004 | Build agents page | [Agent UX](docs/ux/04-agents-page.md) | Developer-validation agents page: install command tabs (`buildInstallCommands`/`renderInstallCommandsPanel`), placement diagnostics panel, fleet table (`renderAgentFleetTable` with health/version/placement); `tests/e2e/ui-smoke.test.mjs`, `tests/unit/ui-helpers.test.mjs`. **External:** live log tail viewer, browser/accessibility matrix, staging agent heartbeat proof. |
| [x] | UX-005 | Build checks library page | [Checks UX](docs/ux/05-checks-library-page.md) | Developer-validation checks library + vector coverage matrix: catalog table with probe profile kinds, safety classes, and family summary; friendly empty state when no target groups; `tests/e2e/ui-smoke.test.mjs` (`GET /v1/checks`). **External:** per-target-group enable/disable toggles, browser/accessibility matrix, staging catalog signoff. |
| [x] | UX-006 | Build test runs page | [Runs UX](docs/ux/06-test-runs-page.md) | Developer-validation runs page: latest run detail with traffic path, verdict explanation, truth table, timeline (`renderRunTimeline`), and metadata-only event list; start-run CTA; `tests/e2e/ui-smoke.test.mjs`. **External:** full run history browser, browser/accessibility matrix, signed-worker staging fidelity. |
| [x] | UX-007 | Build findings page | [Findings UX](docs/ux/07-findings-page.md) | Developer-validation list/detail with **Why this finding?** panel, export + custody preview, friendly empty state, and triage actions (`finding-assign`, `finding-status` accept-risk/close, retest); `tests/unit/verdict-explanation.test.mjs`, `tests/e2e/ui-smoke.test.mjs`. **External:** full SLA/owner workflow, browser/accessibility matrix, staging signoff. |
| [x] | UX-008 | Build reports page | [Reports UX](docs/ux/08-reports-page.md) | Developer-validation report builder (`renderReportBuilder`) for executive/technical/SOC/audit/compliance kinds with JSON/Markdown/HTML export and custody preview; `tests/e2e/ui-smoke.test.mjs`, `tests/unit/ui-helpers.test.mjs`. **External:** PDF export, immutable report storage, browser/accessibility matrix, staging export signoff. |
| [x] | UX-009 | Build SOC console | [SOC Console UX](docs/ux/09-soc-console.md), [SOC Workflow](docs/soc/01-high-scale-ddos-workflow.md) | Developer-validation `#soc` UI: queue, artifact review, schedule, dry-run start/stop, adapter status, SOC note, post-test report, close, kill switch, and action output; backend RBAC/gates remain authoritative. **External:** enterprise IdP/MFA, full production console workflow, live adapter/provider telemetry, and staging/ops signoff. |
| [x] | UX-010 | Build settings/API keys page | [Settings UX](docs/ux/10-settings-page.md) | Bootstrap tokens + compact release-evidence summary on Settings; separate **Support & on-call readiness** panel (`buildSupportReadinessPreview` / `renderSupportReadinessPanel` in `apps/web/ui-helpers.js`, `viewSettings` in `apps/web/app.js`) shows SLA/escalation/SOC reference previews and kill-switch state without implying staffed production on-call; `tests/unit/ui-helpers.test.mjs`, `tests/e2e/ui-smoke.test.mjs`. **External:** users/roles/SSO/integrations, named rotation and SLA custody, staging support signoff. |
| [x] | UX-011 | Add visualizations | [Visualizations](docs/ux/11-visualization-methods.md) | Developer-validation suite in `apps/web/app.js` (`renderReadinessGauge`, `renderReadinessRadar`, `renderVectorHeatmap`, `renderScoreTrend`, `renderTrafficPath`, `renderTruthTable`, `renderRunTimeline`, `renderSocSwimlane`) + CSS viz classes; `tests/e2e/ui-smoke.test.mjs` covers helpers/`misplaced_agent`. **External:** browser/accessibility matrix, live-data fidelity, drill-downs, report parity. |
| [x] | UX-012 | Add friendly empty states | [UX Principles](docs/ux/00-ux-principles.md) | Developer-validation `renderFriendlyEmptyState` + `PAGE_EMPTY_STATES` on Dashboard, Environments, Target Groups, Agents (fleet), Checks (pre-target), Test Runs, Findings, Evidence Vault, High-Scale, Reports, Audit Log, Notifications, Settings tokens, and SOC queue; `tests/unit/ui-helpers.test.mjs`, `tests/e2e/ui-smoke.test.mjs`. **External:** full coverage verified in staging and browser/accessibility matrix. |
| [x] | UX-013 | Add verdict explanation panel | [Enterprise Gap Backlog](docs/product/08-enterprise-production-gap-backlog.md) | Developer-validation **"Why this verdict?"** (Test Runs) and **"Why this finding?"** (Findings detail) panels in `apps/web/verdict-explanation.mjs` (`renderVerdictExplanation`, `renderFindingVerdictExplanation`; Findings loads linked run/events; `tests/unit/verdict-explanation.test.mjs`, `tests/e2e/ui-smoke.test.mjs`) — external probe evidence, internal agent evidence, observation mode, placement confidence (`verdict.placement_confidence` from DET-014), conclusion, remediation (`external_result` at top-level or `metadata.external_result`). **External:** browser/accessibility matrix execution, staging signoff. |
| [x] | UX-014 | Production release evidence visibility | [Settings UX](docs/ux/10-settings-page.md), [Release checklist](docs/release-checklist.md) | Developer-validation **Release Evidence** page (`#release-evidence`) and compact Settings panel via `renderReleaseEvidencePanel` in `apps/web/ui-helpers.js` list accepted kinds, validation summary, release id, created time, and custody URI preview from `GET /v1/production-release-evidence` without raw bodies; gate copy distinguishes repo closed inventory/docs gates (31/31 kinds accepted; gap audit `production_ready=true` when checklist and release-plan rows are closed) from customer-specific launch, retained external signoff, and per-tenant IdP/domain/provider/KMS onboarding — local JSON validation alone is not external signoff. **External:** browser/accessibility matrix execution, staging operator workflow, immutable custody drill signoff. |
| [x] | UX-015 | Build public landing and sign-up/login redirects | [Public Landing and Internal Management](docs/product/13-public-landing-and-internal-management.md), [Page Spec](docs/ux/01-pages-and-tabs.md) | Developer-validation `landing.html` + `signup.html` with `public-site.js`/`signup.js`: no-access-first promise, `Sign up` → `/signup`, `Log in` → `ASTRANULL_PUBLIC_LOGIN_URL` (default `/app`), `GET /v1/public/site-config`; no internal management links; `tests/e2e/ui-smoke.test.mjs`, `tests/integration/public-internal-management-api.test.mjs`. **External:** browser/accessibility matrix, production IdP redirect wiring, staging signoff. |
| [x] | UX-016 | Build staff-only internal management console | [Public Landing and Internal Management](docs/product/13-public-landing-and-internal-management.md), [Page Spec](docs/ux/01-pages-and-tabs.md) | Developer-validation staff console at `/internal/admin` (`internal/admin/index.html`, `internal-admin.js`): overview, sign-up queue approve/reject, tenant detail suspend/reactivate, user invite resend/disable, approval queue, internal audit; staff dev headers / staff OIDC boundary; customer shell has no internal nav. **External:** enterprise staff IdP/MFA, browser/accessibility matrix, staging operator workflow. |

## 2. Backend platform

| Status | ID | Task | Implementation doc | Completion goal |
|---|---|---|---|---|
| [x] | BE-001 | Build tenant/org service | [Backend Architecture](docs/backend/01-backend-architecture.md) | Multi-tenant org and environment model works. |
| [x] | BE-002 | Build auth/RBAC service | [API Design](docs/backend/03-api-design.md), [Security](docs/security/01-security-model.md) | RBAC enforced on sensitive routes; built-in **`oidc-jwt`** human auth (RS256/JWKS, production default; `dev-headers`/`signed-session` refused in production); scoped service-account tokens (`svc_`) including addressed `svc_v1.…`; invalid addressed service-account bearer audits safely; bootstrap issuance uses addressed `ast_v1.…`; `signed-session` for non-production tests/operators only. `scripts/oidc-prod-auth-preflight.mjs` / `npm run oidc:prod:preflight` captures offline metadata-only production-auth posture evidence. Postgres `authTokenRepository` primitives and `createPostgresAuthServices` (`tokens`/`serviceAccounts` on `createPostgresRuntime().services`) are injected by Postgres startup for migrated auth routes; RBAC denials in Postgres mode use `runtime.services.audit`. **External:** real IdP tenant/role mapping, MFA/conditional access, staging login flow, header-only negative test in prod-like deployment, full guarded-route migration, audit/ops signoff. |
| [x] | BE-003 | Build bootstrap-token service | [Agent Registration Flow](docs/flows/02-agent-registration.md), [API Key Lifecycle](docs/agent/03-api-key-and-token-lifecycle.md) | One-time/expiring install tokens are generated, displayed once, revoked, audited. |
| [x] | BE-004 | Build agent registry | [Agent Architecture](docs/agent/01-agent-architecture.md) | Agents register, receive identity, send heartbeat, report capabilities. Postgres `createPostgresAgentServices` on `createPostgresRuntime().services` (`agents` + `agentAuth`; addressed `agc_` only; `tests/unit/postgres-service-adapters.test.mjs`) is injected by Postgres startup for migrated agent routes. |
| [x] | BE-005 | Build target group APIs | [Target Groups](docs/product/04-target-groups.md), [API Design](docs/backend/03-api-design.md) | CRUD for target groups and nested targets: `POST/PATCH/DELETE /v1/target-groups/:id`, `POST/PATCH/DELETE /v1/target-groups/:id/targets/:targetId`; archive rejects active runs (`409 target_group_active_run`); Postgres `coreCatalogRepository` parity; `tests/integration/target-groups-api.test.mjs`. |
| [x] | BE-006 | Build check catalog service | [Check Library](docs/detection/02-check-library.md) | Checks are versioned, categorized, documented, and gated by safety class. |
| [x] | BE-007 | Build test planner | [Test Orchestration](docs/backend/02-test-orchestration.md) | Planner creates safe test plan from target group + enabled checks; Postgres `runtime.services.testRuns.startTestRun` now enforces runnable check, declared target group/target, prerequisite agents, safe windows, rate/cooldown limits, concurrent-run block, tenant kill switch, audit, and agent/probe job dispatch. **External:** live/staging Postgres acceptance execution evidence, gated `postgres-runtime-smoke` run in staging, concurrent tenant isolation under load, and staging signed-worker matrix (safe validation loop start/probe/observation/finalization/verdict/finding publication is wired in Postgres mode). |
| [x] | BE-008 | Build probe coordinator | [Test Orchestration](docs/backend/02-test-orchestration.md) | Signed `probeJobs` + worker poll/result routes + bounded `workers/probe-worker.mjs` CLI; Postgres mode uses `runtime.services.probeJobs` for tenant-scoped worker lease/result ingestion and requires signed `x-probe-tenant-id`; `runtime.services.testRuns.startTestRun` creates signed worker jobs without exposing nonce in API responses; `scripts/probe-fleet-matrix-evidence.mjs` / `npm run probe:fleet:matrix:evidence` validates metadata-only multi-region signed-worker fleet evidence. Simulation remains default for dev/CI and is refused when `NODE_ENV=production`; multi-region fleet deploy/staging E2E still not production-ready. |
| [x] | BE-009 | Build agent control channel | [Control Plane](docs/backend/04-control-plane.md) | Agent receives jobs over outbound channel; no inbound management required. |
| [x] | BE-010 | Build event ingestion | [Events & Queues](docs/backend/06-events-and-queues.md) | Probe, agent, health, SOC, and evidence events ingested with tenant isolation. |
| [x] | BE-011 | Build correlation engine | [Correlation Engine](docs/detection/03-correlation-engine.md) | Probe events and agent observations correlate by nonce, target, and time window. |
| [x] | BE-012 | Build verdict engine | [Evidence & Scoring](docs/detection/04-evidence-and-scoring.md) | Produces pass/fail/inconclusive/misplaced-agent/bypass verdicts. |
| [x] | BE-013 | Build findings service | [Evidence & Scoring](docs/detection/04-evidence-and-scoring.md) | Failing verdicts create deduplicated findings with severity and remediation. |
| [x] | BE-014 | Build report generator | [Reporting](docs/ux/08-reports-page.md) | JSON/Markdown/HTML self-contained export with redaction and SOC notes (PDF not shipped). |
| [x] | BE-015 | Build audit log service | [Security Model](docs/security/01-security-model.md) | Tamper-evident hash chain (sequence/prev_hash/entry_hash), no log truncation; report and finding exports audited in dev JSON via `audit()`; shared record builder `buildAuditRecord` in `src/audit.mjs` (`tests/unit/audit-chain.test.mjs`). Postgres `auditRepository.appendAuditEvent` / `listAuditEntries` are exposed as `runtime.services.audit`; Postgres mode uses them for RBAC-denial auditing and `GET /v1/audit-log` without dev-store fallback. |
| [x] | BE-016 | Build notification service | [Notifications](docs/backend/07-notifications.md) | Validated channels/triggers/destinations, redacted metadata, per-event `delivery_attempts` ledger, audits `notification.rule_created` / `notification.event_emitted` / `notification.delivery_attempt_recorded`, safe-by-default (no external sends unless `ASTRANULL_NOTIFICATION_DELIVERY_MODE` includes channel); opt-in webhook delivery via `src/lib/notificationDelivery.mjs` (HTTPS-only remote, bounded timeout/payload, no redirects/URL credentials, redacted JSON body, `delivered_provider` / `provider_retry_scheduled` / `provider_failed_dlq` metadata). **Email adapter** (`buildEmailPayload`/`deliverEmail`): SMTP via `node:net`/`node:tls` with STARTTLS, bounded timeout, redacted HTML body. **Slack adapter** (`buildSlackPayload`/`deliverSlack`): Block Kit JSON via HTTPS webhook. **Teams adapter** (`buildTeamsPayload`/`deliverTeams`): Adaptive Card JSON via HTTPS webhook. Delivery mode parsing accepts comma-separated values (`webhook,email,slack,teams,all,metadata_only`). 11 adapter tests + 9 existing notification tests pass. `POST /v1/notifications` maps invalid rules to HTTP 400. Postgres `notificationRepository` + `createPostgresNotificationServices` share the same delivery helper with DI for tests. Service-level due-retry executor `src/lib/notificationRetry.mjs` + `processDueNotificationRetries` (dev JSON + Postgres adapters); externally scheduled Postgres runner `scripts/notification-retry-runner.mjs` / `npm run notification:retry:runner` (default metadata-only ledger updates). Postgres `notification_delivery_attempts` persist retry/DLQ metadata (`attempt_number`, `max_attempts`, `next_retry_at`, `provider_error`, `exhausted`, `provider_status`) via migration `0010`; retry runtime handles combined delivery modes and performs bounded adapter I/O for active webhook/email/slack/teams channels; evidence worker uses latest attempt per rule. Notifications page shows retry/DLQ visibility and `POST /v1/notifications/retries/process` runs metadata-only due-retry processing in developer validation. **External:** always-on persistent retry scheduler, per-tenant encrypted provider credentials, SMTP AUTH support, provider delivery staging evidence. |
| [x] | BE-017 | Build sign-up intake and tenant provisioning backend | [Public Landing and Internal Management](docs/product/13-public-landing-and-internal-management.md), [API Design](docs/backend/03-api-design.md) | Public `POST/GET /v1/signup-requests`, state machine, dedupe/rate limits, staff approve/reject, tenant/env/owner provisioning, default subscription/entitlements/retention, and internal audit are implemented for dev-json and Postgres (`0021_internal_management`, `createPostgresInternalManagementServices`, `runtime.services.signupIntake`); `tests/integration/public-internal-management-api.test.mjs`, `tests/unit/signup-intake.test.mjs`, `tests/unit/postgres-internal-management-service-adapters.test.mjs`, `tests/unit/server-postgres-mode.test.mjs`. **External:** notification delivery for approval/rejection notices, staging public-signup abuse-controls evidence, production IdP/staff workflow signoff. |
| [x] | BE-018 | Build staff-only internal management backend | [Public Landing and Internal Management](docs/product/13-public-landing-and-internal-management.md), [Security](docs/security/01-security-model.md) | Staff RBAC (`staffRoles`, `staffAuth`, `staffRbac`) on `/internal/admin/*` routes covers overview, sign-ups, tenants, subscription/entitlements, support actions, approvals, and internal audit; Postgres mode uses injected `runtime.services.internalManagement` instead of `postgres_internal_admin_not_wired`; subscription limits remain enforced in `startTestRun` (suspended tenant + plan caps); customer principals denied; `tests/integration/public-internal-management-api.test.mjs`, `tests/unit/postgres-internal-management-service-adapters.test.mjs`, `tests/unit/server-postgres-mode.test.mjs`. **External:** production staff IdP/MFA, separate internal host/edge controls, browser/accessibility matrix, and staging security/operator signoff. |


## 3. Agent engineering

| Status | ID | Task | Implementation doc | Completion goal |
|---|---|---|---|---|
| [x] | AG-001 | Build Linux host agent base | [Agent Architecture](docs/agent/01-agent-architecture.md) | Node metadata agent + systemd/Docker paths ship; packaged control-plane URL defaults to HTTPS (localhost HTTP requires `--allow-insecure-localhost-api` or `ASTRANULL_ALLOW_INSECURE_LOCALHOST_API=1`); packaged identity defaults to `/var/lib/astranull/identity.json` (`0700` dir / `0600` file); packaged tarball source-isolation test blocks server-side `src/*` imports; invalid addressed `agc_v1` auth audits `agent.auth_denied` only when a matching `(tenant_id, agent_id)` row exists (nonexistent/mismatched hints and unknown legacy route agents return `401` without tenant-local audit). `scripts/agent-mtls-gateway-evidence.mjs` / `npm run agent:mtls:evidence` validates metadata-only production gateway mTLS deployment evidence (`agent_mtls_gateway` release kind) and rejects PEM bodies, private keys, tokens, and other forbidden content. **External:** live gateway/proxy mTLS deployment, PKI issuance/rotation/revocation drill execution, staging registration/heartbeat proof, security signoff. Static binary per-distro matrix remains. |
| [x] | AG-002 | Build one-line installer | [Agent Installation](docs/agent/02-installation-and-packaging.md) | Generic SHA-256-verified install path plus `scripts/package-agent.mjs` signed generic tarball builder (manifest + optional Ed25519 `.manifest.json.sig`) and native deb/rpm builders (`agent:package:native`). `scripts/agent-install-matrix-evidence.mjs` / `npm run agent:install:matrix:evidence` validates metadata-only install/revoke/uninstall matrix evidence. **External:** installer auto-select of signed deb/rpm from hosted apt/yum repos, full installer-side trust-key enforcement, GPG-signed native packages, real distro/Kubernetes matrix execution. |
| [x] | AG-003 | Build deb package | [Agent Installation](docs/agent/02-installation-and-packaging.md) | `scripts/package-agent.mjs --format deb` builds pure Node `.deb` (systemd unit, `agent.env.example`, `/var/lib/astranull/`, prerm/postinst); `pnpm run agent:package:native`; covered by `tests/unit/agent-package.test.mjs`. **External:** apt repo publishing, GPG signing, Ubuntu/Debian matrix validation, hosted custody — not `[x]` without staging evidence. |
| [x] | AG-004 | Build rpm package | [Agent Installation](docs/agent/02-installation-and-packaging.md) | `scripts/package-agent.mjs --format rpm` invokes `rpmbuild` when available; `--rpm-spec-only` for CI without `rpmbuild`; same install paths and safety guarantees as deb. **External:** yum/dnf repo publishing, GPG signing, RHEL/Amazon Linux/SUSE matrix validation, hosted custody — not `[x]` without staging evidence. |
| [x] | AG-005 | Build tarball fallback | [Agent Installation](docs/agent/02-installation-and-packaging.md) | `agent:package` / `agent:package:all` / `scripts/package-agent.mjs` builds generic Linux `.tar.gz`, manifest, optional Ed25519 signature, and native deb/rpm when `--format` includes them. `scripts/agent-sbom-provenance-evidence.mjs` / `npm run agent:sbom:provenance:evidence` validates package/SBOM/provenance metadata without executing artifacts. **External:** hosted download, installer-side manifest verification tied to tenant trust keys on hosts, distro validation matrix. |
| [x] | AG-006 | Build Docker image | [Agent Installation](docs/agent/02-installation-and-packaging.md) | Non-root Alpine image (`agents/linux/Dockerfile`, `.dockerignore`, OCI labels, no baked bootstrap token or dev-only API default); `scripts/agent-container-evidence.mjs` + `tests/unit/agent-container.test.mjs` validate Dockerfile and image verify commands. **External:** customer registry publish, cosign/image signing, staging canary container E2E. |
| [x] | AG-007 | Build Helm chart | [Kubernetes Deployment](docs/agent/05-kubernetes-agent.md) | DaemonSet/deployment modes, optional `bootstrapToken` Secret template, file-mounted bootstrap token, non-root securityContext, resource limits, opt-in canary listener, and durable `/var/lib/astranull` identity storage (hostPath default; PVC/emptyDir options); `scripts/render-agent-helm.mjs` + `tests/unit/agent-helm.test.mjs` (static + `helm template` when available). **External:** charts.astranull.example publish, staging DaemonSet/canary rollout drill, sidecar examples. |
| [x] | AG-008 | Build agent registration | [Agent Registration Flow](docs/flows/02-agent-registration.md) | Bootstrap token exchanged for per-agent identity. |
| [x] | AG-009 | Build heartbeat | [Agent Architecture](docs/agent/01-agent-architecture.md) | Agent reports version, capabilities, placement, health, and clock skew. |
| [x] | AG-010 | Build local packet metadata observation | [Agent Detection Modes](docs/agent/04-detection-modes.md) | Linux agent uploads observations only when a matching local signal exists; `--packet-metadata-file` metadata-only JSONL ingestion (raw/payload/header/log fields ignored; control plane rejects them). **External:** signed packages, staging E2E on customer host/interface paths. |
| [x] | AG-011 | Build canary HTTP listener mode | [Agent Detection Modes](docs/agent/04-detection-modes.md) | Opt-in canary listener records safe metadata only; evidence-gated observation upload; customer approval documented. |
| [x] | AG-012 | Build log-tail observation mode | [Agent Detection Modes](docs/agent/04-detection-modes.md) | Log-tail mode hashes lines locally; evidence-gated upload; no raw log upload; placement guide linked in UI. |
| [x] | AG-013 | Build packet-mirror collector mode | [Placement Guide](docs/agent/06-placement-guide.md) | `--mirror-metadata-file` metadata-only JSONL ingestion; evidence-gated uploads; raw fields ignored. **External:** customer TAP/mirror setup, staging validation on cloud/on-prem mirror paths. |
| [x] | AG-014 | Build auto-update mechanism | [Agent Lifecycle](docs/agent/07-agent-lifecycle.md) | Control plane: `POST/GET /v1/agent-updates`, `POST /v1/agent-updates/:id/rollback` (`agent_update:*` RBAC); `POST/GET /v1/agent-update-trust-keys`, `POST /v1/agent-update-trust-keys/:id/revoke` (`agent_update:write`/`agent_update:read`) — tenant trust-key ledger (DER SPKI Ed25519, `fingerprint_sha256`, duplicate-active reject, revoke); release/rollback creation requires manifest signing key matching an active tenant trust key (`untrusted_signing_key` otherwise); release/status ledgers; shared `src/lib/agentUpdates.mjs` verification for signed `astranull-agent` manifests (Ed25519 detached sig, SHA-256/size, safe `.tar.gz` basename); deterministic staged rollout by tenant/agent/version percentage plus optional environment/target-group/agent filters; `GET /v1/agents/:id/update` and `POST /v1/agents/:id/update-status` (upgrade/rollback poll, status ack, version update on `applied`/`rolled_back`, `agent_update.status_recorded` audit); audits `agent_update.trust_key_added` / `agent_update.trust_key_revoked`; release/rollback require `distribution: { manifest_url, signature_url, artifact_url }` (absolute HTTPS; URL credentials rejected; `artifact_url` basename must match `manifest.artifact.name`; query strings allowed); `GET /v1/agents/:id/update` returns `download` for upgrade/rollback; audit metadata excludes distribution URLs. Postgres mode persists the route family through `agentUpdateRepository` + `createPostgresAgentUpdateServices` as `runtime.services.agentUpdates` and fails closed if the service is missing. Agent CLI `--verify-update-manifest` preflight, host-side `--apply-update-manifest`, and `--download-and-apply-update` (HTTPS-only download policy by default; localhost HTTP only with `--allow-insecure-localhost-downloads`; rejects URL credentials; bounded response size and timeout; HTTP redirects not followed; manifest/signature/artifact verification gates apply) in `agents/linux/astranull-agent.mjs`. Tests: `tests/unit/agent-updates.test.mjs`, `tests/unit/postgres-agent-update-repository.test.mjs`, `tests/integration/agent-updates-api.test.mjs`, `tests/unit/agent-package.test.mjs`, `tests/unit/agent-runtime.test.mjs`, `tests/unit/agent-trust-key-ceremony-evidence.test.mjs`. Trust-key ceremony/rotation/revocation drill evidence validator: `scripts/agent-trust-key-ceremony-evidence.mjs` / `npm run agent:trust-key:evidence` (metadata-only; production release kind `agent_trust_key_ceremony`). **External:** hosted artifact storage custody/CDN/mirror operational runbooks and staging evidence, unattended daemon orchestration and service restart, live staging trust-key drill execution and installer/update-daemon enforcement of pinned trust keys, deb/rpm/container rollout integration, staging distro/Kubernetes rollout + rollback drill, enterprise IdP/MFA around human release admin. |
| [x] | AG-015 | Build safe revoke/uninstall | [Agent Installation](docs/agent/02-installation-and-packaging.md) | `POST /v1/agents/:id/revoke` (`agent:revoke`) marks agents revoked, audits `agent.revoked`, and rejects the old credential on heartbeat, jobs, observations, and update poll/status routes in developer JSON and Postgres service modes. `agents/linux/uninstall.sh`: `--dry-run`, absolute `--install-root`, idempotent removal, optional `--purge-data`; rejects relative install roots; staged tests. Native deb/rpm `prerm` stops systemd before package-manager remove. **External:** staging revoke/uninstall drills, signed apt/yum repo publishing, distro matrix uninstall validation, default purge semantics for `/var/lib/astranull` on package remove. |
| [~] | AG-016 | Token-validated `probe_endpoint` reporting | [Deployment Modes and Onboarding](docs/agent/09-deployment-modes-and-onboarding.md) | Agent builds and sends `probe_endpoint` on heartbeat; `validateProbeEndpoint` + `checkProbeEndpointBinding` (gates 6–7) in dev and Postgres modes; persists `probe_endpoint`/`probe_endpoint_status`/`probe_endpoint_error` and `last_token_validation_*`; probe scheduling excludes `token_valid === false`. **Postgres binding gate 6–7 fixed** (`agentServiceAdapters` wires token/target repo). **UI added:** agent detail surfaces `probe_endpoint_status`, `probe_endpoint_error`, and declared FQDN/IP from `probe_endpoint`. Tests: `probe-endpoint-*`, `agent-probe-endpoint-api`, postgres repo tests. **External:** staging heartbeat/probe matrix on customer edges. |
| [x] | AG-017 | Ownership verification (Mode A1, no inbound) | [Deployment Modes and Onboarding](docs/agent/09-deployment-modes-and-onboarding.md) | `ownership_challenge` signed probe jobs; `recordOwnershipSignal` auto-correlates probe + agent observations → `agent_verified`; `confirmOwnership` → `user_confirmed`; abuse guardrails (bound online token-valid agent, declared FQDN in group); Postgres migration `0024_ownership_verifications.sql` + service adapters; routes `POST/GET /v1/ownership-verifications`, confirm, `POST /v1/ownership-verifications/verify-setup`; target-group UI (DNS TXT, validation mode, ownership confirm, verify-setup). Tests: `ownership-verification.test.mjs`, `ownership-verification-api`, `probe-coordinator`, `probe-worker`, postgres adapters. **External:** staging live probe + observation correlation at scale. |
| [x] | AG-018 | External-only validation mode | [Deployment Modes and Onboarding](docs/agent/09-deployment-modes-and-onboarding.md) | `validation_mode` (`agent_assisted`/`external_only`) on target groups; `dnsOwnership` TXT issue/verify → `dns_verified`; `correlateExternalOnlyVerdict` + finalize branch (`confidence: external_only`, `placement: unverified`, `strengthen_hint: deploy_agent`); DNS-ownership routes; target-group detail validation-mode toggle + DNS TXT UI; external-only finding "Strengthen" CTA. Tests: `target-groups-validation-mode`, `dns-ownership`, `correlation`. **External:** staging signed-worker scans on customer-declared edges. |
| [~] | AG-019 | Hardened image profile + low-friction onboarding | [Deployment Modes and Onboarding](docs/agent/09-deployment-modes-and-onboarding.md) | **In-repo slivers [x]:** pre-bound tokens (`prebind_fqdn`, `deployment_packaging`); agent opt-in cloud-metadata public-IP discovery (`discovered_via: cloud_metadata`); `verifyOwnershipSetup` + `POST /v1/ownership-verifications/verify-setup` + target-group "Verify my setup" UI; agent detail probe-endpoint status surfaces. **[!] External:** publish cosign-signed `astranull/agent` distroless image (read-only-root, `cap_drop: ALL`, non-root) with SBOM/provenance; auto baseline on `agent_verified`; CI secret-scan on image layers. |

## 4. Detection and vectors

| Status | ID | Task | Implementation doc | Completion goal |
|---|---|---|---|---|
| [x] | DET-001 | Implement direct-origin bypass checks | [Origin Bypass](docs/detection/05-origin-bypass.md) | Safe-check catalog entry declares `supported_targets`, `required_customer_setup`, `evidence_required`, `verdict_logic`, and bounded `probe_profile`; signed jobs carry probe profile metadata (tamper with `probe_profile` or target `port` invalidates signature); worker sends bounded metadata probes. Unit/integration tests cover vectors, signed jobs, worker metadata, port propagation, and safety scan. **External:** staging signed-worker fleet matrix and live-target acceptance per vector doc. |
| [x] | DET-002 | Implement protected-path canary checks | [Check Library](docs/detection/02-check-library.md) | Same catalog/probe-profile contract and signed bounded worker path as DET-001 for this vector family. **External:** staging signed-worker fleet matrix and live-target acceptance. |
| [x] | DET-003 | Implement forbidden port exposure checks | [L3/L4 Vectors](docs/detection/06-l3-l4-vectors.md) | Catalog + bounded `probe_profile` (TCP one connect); signed jobs preserve target `port`/`protocol`. Tests in CI; not full staging fleet matrix. **External:** staging signed-worker fleet validation and agent observation correlation at scale. |
| [x] | DET-004 | Implement basic deny-rule validation | [L3/L4 Vectors](docs/detection/06-l3-l4-vectors.md) | Catalog + bounded probe profile and signed worker routing landed with tests. **External:** live deny/penetrate correlation evidence on customer paths in staging. |
| [x] | DET-005 | Implement DNS exposure checks | [DNS Vectors](docs/detection/07-dns-vectors.md) | Catalog + bounded probe profile (DNS lookup only); signed job metadata enforced. **External:** staging signed-worker fleet and resolver/path acceptance matrix. |
| [x] | DET-006 | Implement HTTP method/header checks | [L7/API Vectors](docs/detection/08-l7-api-vectors.md) | Catalog + bounded probe profile (HTTP HEAD); signed payloads include probe profile. **External:** staging fleet E2E and customer path validation. |
| [x] | DET-007 | Implement WAF marker checks | [L7/API Vectors](docs/detection/08-l7-api-vectors.md) | Catalog definitions and bounded signed probe path with tests. **External:** customer marker rule validation in staging against real WAF edges. |
| [x] | DET-008 | Implement low-rate rate-limit checks | [L7/API Vectors](docs/detection/08-l7-api-vectors.md) | Catalog + bounded profiles; worker remains low-volume by contract. **External:** threshold behavior evidence on customer limits in staging (not load test). |
| [x] | DET-009 | Implement TLS/connection checks | [TLS & Connection Vectors](docs/detection/09-tls-connection-vectors.md) | Catalog + bounded connection probe profile and signed jobs. **External:** staging signed-worker fleet and handshake/timeout acceptance on declared targets. |
| [x] | DET-010 | Implement HTTP/2 and HTTP/3 readiness checks | [Protocol Vectors](docs/detection/10-protocol-vectors.md) | Catalog + metadata-marker/no-network or bounded probe profiles per check; signed job contract. **External:** staging protocol readiness matrix on customer edges. |
| [x] | DET-011 | Implement API expensive-endpoint checks | [L7/API Vectors](docs/detection/08-l7-api-vectors.md) | Catalog + bounded probe profile and verdict logic declared; CI vector/profile tests. **External:** expensive-endpoint validation on customer-declared APIs in staging. |
| [x] | DET-012 | Implement high-scale detection telemetry model | [High-Scale DDoS](docs/detection/11-high-scale-ddos-detection.md) | Metadata-only SOC telemetry routes reject raw/payload fields, audit `high_scale.telemetry_recorded`, feed safe `telemetry_summary` into post-test reports, and are backed by Postgres `highScaleRepository` / `runtime.services.highScale` in Postgres mode. **External:** live provider/staging telemetry feeds and automated governed-adapter ingestion. |
| [x] | DET-013 | Implement misplaced-agent detection | [Correlation Engine](docs/detection/03-correlation-engine.md) | `src/services/placement.mjs` per-target-group statuses (`proven`, `needs_baseline`, `missing_agent`, `misplaced_risk`); readiness placement factor includes diagnostics summary and does not credit unbound online agents alone. **External:** UI surfacing, staging/live baseline evidence. |
| [x] | DET-014 | Implement agent confidence score | [Enterprise Gap Backlog](docs/product/08-enterprise-production-gap-backlog.md), [Placement Guide](docs/agent/06-placement-guide.md) | `computePlacementConfidence` in `src/services/placement.mjs` attaches `placement_confidence` on finalized verdicts (`finalizeVerdictIfReady`); JSON report export includes field; Test Runs panel uses backend reason/level. Tests: `tests/unit/placement-confidence.test.mjs`, probe-coordinator/first-slice/ui-smoke. **External:** staging/live validation across host/sidecar/canary/mirror/log modes, browser/accessibility matrix, staging/live DB evidence for verdict/report export paths (schema/repository parity via `0005_verdict_placement_confidence` landed), customer installation matrix proof. |
| [x] | DET-015 | Expand vector backlog to enterprise families | [Vector Test Matrix](docs/detection/12-vector-test-matrix.md), [Enterprise Gap Backlog](docs/product/08-enterprise-production-gap-backlog.md) | `CHECK_CATALOG` in `src/contracts/checks.mjs` expanded with enterprise L3/L4 (IPv6 safe, connection exhaustion SOC), DNS (amplification metadata, DNSSEC, failover, AXFR), L7/API (cache bust, expensive endpoint, auth flows, quota, GraphQL, bot marker), TLS timeouts, modern protocols (HTTP/2 rapid reset/stream, gRPC, WebSocket), and SOC-only ops/high-scale request markers; `stop_conditions` + governance metadata on all entries; `tests/unit/vectors.test.mjs` enterprise ID assertions; `scripts/vector-safety-policy-evidence.mjs` / `npm run vector:safety:evidence` validates metadata-only catalog policy completeness without traffic generation. **[?]** `protocol.grpc_reflection_stream.safe` remains `metadata_marker`-only (protobuf+h2 stream framing deps deferred; see `docs/progress-detailed.md`). **External:** staging/live probe-worker execution per new `probe_profile` kinds where not `metadata_marker`, UI catalog surfacing, and matrix signoff against customer installations. |

## 5. SOC and high-scale workflow

| Status | ID | Task | Implementation doc | Completion goal |
|---|---|---|---|---|
| [x] | SOC-001 | Build high-scale request form | [High-Scale Workflow](docs/soc/01-high-scale-ddos-workflow.md) | Production-shaped intake API + High-Scale page form (`scope_confirmation`, window, contacts, provider context). **External:** full staged customer workflow, durable document store, legal/offline signoff, browser accessibility matrix. |
| [x] | SOC-002 | Build authorization document upload | [Authorization Pack](docs/soc/02-authorization-pack.md) | Developer-validation metadata-only artifact upload persists `content_sha256`, `custody_id`, `custody_uri`, upload envelope, review state, and audit metadata in dev-json and Postgres `authorization_artifacts`; raw document/file fields are rejected; SOC review and approval gating use the persisted pack status. Tests: `tests/integration/hardening.test.mjs`, `tests/unit/postgres-high-scale-repository.test.mjs`, `tests/unit/postgres-high-scale-service-adapters.test.mjs`, `tests/unit/authorization-custody-evidence.test.mjs`. **External:** immutable/KMS-backed artifact retention, legal custody/export signoff, staging workflow evidence, and live provider integrations. |
| [x] | SOC-003 | Build provider approval checklist | [Authorization Pack](docs/soc/02-authorization-pack.md) | `provider_approval_checklist` on high-scale requests (metadata derive/upload/review/expiry gates SOC approve); `scripts/provider-approval-evidence.mjs` / `npm run soc:provider-approval:evidence` validates metadata-only provider approval path evidence; integration tests in `hardening.test.mjs`. **External:** live provider integration, staging evidence execution, export/signoff. |
| [x] | SOC-004 | Build SOC approval state machine | [High-Scale Workflow](docs/soc/01-high-scale-ddos-workflow.md) | Two-person SOC approval, declared-scope hash locking/revalidation, stopped-only close (post-test report required via `post_test_report_required`), state-change notifications, denial audits, and kill-switch auto-stop are enforced in dev and Postgres-backed services. **External:** staging SOC drill, enterprise IdP/MFA workflow, legal/ops signoff. |
| [x] | SOC-005 | Build execution console | [SOC Console UX](docs/ux/09-soc-console.md) | Developer-validation console surfaces queue, dry-run adapter start/stop, adapter status, kill switch, SOC notes, post-test report (including safe `telemetry_summary`), close, and action output; SOC-only metadata telemetry route exists (no production/live adapter implied). **External:** live health, external availability, agent observations, and provider telemetry adapter in governed execution. |
| [x] | SOC-006 | Build kill switch | [High-Scale Workflow](docs/soc/01-high-scale-ddos-workflow.md) | Backend tenant-aware kill switch stops governed high-scale adapter runs, audits adapter stop + auto-stop, cancels active safe test runs (`test_run.kill_switch_auto_cancel`), and in Postgres mode cancels open signed-worker probe jobs for those runs (`probe_job.kill_switch_auto_cancel`; response and `soc.kill_switch.activated` audit include `cancelled_probe_job_ids`). Persisted through Postgres `killSwitchRepository` / `runtime.services.highScale` + `probeJobsRepo.cancelOpenProbeJobsForTestRuns`. `scripts/kill-switch-drill-evidence.mjs` / `npm run soc:kill-switch:evidence` validates metadata-only drill transcripts and latency. **External:** live external signed-worker fleet stop validation (staging/live fleet evidence) and staging kill-switch drill execution — Postgres job cancellation alone is not fleet stop proof. |
| [x] | SOC-007 | Build partner adapter interface | [High-Scale Execution Adapter](docs/soc/03-execution-adapter.md) | Governed adapter start/stop with audit; dry-run boundary only until partner integration certified. `scripts/governed-adapter-evidence.mjs` / `npm run soc:adapter:evidence` validates metadata-only adapter readiness evidence while rejecting raw commands, payloads, target IP inventories, and credentials. Telemetry/evidence boundary remains dry-run/manual SOC entry until governed partner adapter certified for live feeds. |
| [x] | SOC-008 | Build post-test report | [High-Scale Workflow](docs/soc/01-high-scale-ddos-workflow.md) | SOC-only `POST/GET /internal/soc/high-scale/:id/post-test-report`; close gated on stored report (`post_test_report_required`); metadata-only derived timeline/artifacts/SOC notes/adapter/telemetry summary with redaction, backed by Postgres high-scale report storage in Postgres mode. **External:** full SOC report workflow/UI, legal evidence retention/export signoff, and staging/ops evidence per release checklist. |
| [x] | SOC-009 | Build authorization proof workflow | [Enterprise Gap Backlog](docs/product/08-enterprise-production-gap-backlog.md), [Authorization Pack](docs/soc/02-authorization-pack.md), [High-Scale Flow](docs/flows/04-high-scale-request-and-approval.md) | High-Scale UI collects required intake (environment, criticality, scenario families, safe `requested_limits`, stop/abort criteria); per-type metadata artifact upload helpers; displays `authorization_pack_status` when present. Backend enforces required intake, expanded required artifact types, computes `authorization_pack_status`, gates SOC `approve`, and Postgres mode persists/revalidates the guarded route family with developer-validation custody ledger support. **External:** real provider integration, immutable/KMS production custody, staging/legal signoff, browser/accessibility matrix. |
| [x] | SOC-010 | Encode provider-specific approval paths | [Enterprise Gap Backlog](docs/product/08-enterprise-production-gap-backlog.md), [Execution Adapter](docs/soc/03-execution-adapter.md) | Developer-validation: `src/contracts/providerApprovalPaths.mjs` models AWS/Azure/GCP/CDN/carrier/on-prem/partner paths with accepted path labels, required evidence, limits, stop controls, and SOC/customer summaries; high-scale provider checklists carry provider metadata and stay `partial` until required approval fields are present; tests cover provider normalization and approval gating. **External:** live provider integrations, durable approval-document custody, staging provider drills, and legal retention/export signoff. |

## 6. Security, compliance, and reliability

| Status | ID | Task | Implementation doc | Completion goal |
|---|---|---|---|---|
| [x] | SEC-001 | Implement tenant isolation | [Security Model](docs/security/01-security-model.md) | Forced RLS schema, migrations `0001`-`0007`, `withTenantContext`, static schema checks, repository primitives, migration runner, startup preflight, gated `postgres-acceptance` harness, and gated `postgres-runtime-smoke` DB-backed runtime wiring harness (`scripts/postgres-runtime-smoke.mjs`, `tests/unit/postgres-runtime-smoke.test.mjs`) are implemented. `scripts/postgres-concurrency-evidence.mjs` / `npm run postgres:concurrency:evidence` validates captured staging concurrency/isolation evidence without connecting to the DB. Static Postgres tenant-query audit (`scripts/postgres-tenant-query-audit.mjs` / `npm run postgres:tenant-query:audit`, `make verify` / CI) scans migrated repositories/adapters for tenant-scoped SQL without tenant context and can emit contract-valid `postgres_tenant_query_audit` release evidence (`buildProductionTenantQueryAuditEvidence`, `tests/unit/postgres-tenant-query-audit.test.mjs`). `createPostgresRuntime` now initializes in `src/startup.mjs` for `ASTRANULL_PERSISTENCE_MODE=postgres`, injects migrated services into `createServer`, uses `runtime.health()` for `/ready`, skips dev seeding, and closes the pool on shutdown/failure. Migrated services cover catalog/auth/agent/agent updates/validation reads, safe test-run start/cancel and signed probe dispatch, event ingestion, notifications, evidence/findings, production release evidence, secret vault, report exports, dashboard state summaries with high-scale/kill-switch posture, probe worker lease-result ingestion, safe-run kill-switch checks, guarded high-scale/SOC workflows, audit-log reads, and RBAC-denial audit. **External:** tenant unit-of-work review on all queries; live/staging DB acceptance and runtime-smoke execution evidence; concurrent tenant isolation under load. |
| [x] | SEC-002 | Implement secret handling | [Security Model](docs/security/01-security-model.md) | AES-256-GCM envelopes (`src/lib/secrets.mjs`), `secretVault` store/list/rotate/decrypt-for-use, `POST/GET /v1/secrets`, `POST /v1/secrets/:id/rotate`, `secret:read`/`secret:write`/`secret:rotate` RBAC; bootstrap, service-account, and agent one-time secrets issued as addressed `ast_v1.…` / `svc_v1.…` / `agc_v1.…`; service-account rotation invalidates old bearer; redaction and parser rejection covered by tests. Postgres auth services are injected for bootstrap/service-account routes; `secretVaultRepository` + `createPostgresSecretVaultServices` persist envelopes under RLS and inject `runtime.services.secretVault` for `/v1/secrets`; `scripts/secret-rotation-drill-evidence.mjs` / `npm run secret:rotation:evidence` validates metadata-only rotation drill evidence and rejects plaintext/ciphertext/credential fields. **External:** external KMS/HSM or approved vault, envelope automated rotation policy, key-rotation drill execution, staging/live DB evidence. |
| [x] | SEC-003 | Implement safe-test guardrails | [Safe Testing](docs/security/02-safe-testing-policy.md) | Done in dev/Postgres: target group safety policy, safe windows, tenant rate budget, group cooldown, per-run event caps, cancellable-state guard, SOC kill switch deny/auto-cancel of safe runs, Postgres kill-switch path also cancels open signed-worker probe jobs for auto-cancelled runs (`probe_job.kill_switch_auto_cancel`, `cancelled_probe_job_ids` on activation), recursive agent observation raw-field denylist, signed probe job `max_requests`/`timeout_ms` caps + `safety_attestation` enforced at probe result ingest (`probe-coordinator.test.mjs`), audits + unit tests (`tests/unit/postgres-high-scale-service-adapters.test.mjs`). **External:** staging/live signed-worker fleet stop-path evidence; control-plane cancellation does not replace fleet matrix validation. |
| [x] | SEC-004 | Implement privacy controls | [Privacy & Data](docs/security/03-privacy-and-data-retention.md) | Agent observation ingest is job-bound with metadata-only + redaction; tenant metadata retention/purge (`metadata_retention_days` clamp, `privacy.retention_purged` audits); `privacy_settings.evidence_retention` normalized (defaults/clamps: `audit_log_days`/`high_scale_artifact_days` 2555, `report_days` 365, `legal_hold` false); `buildRetentionPolicySnapshot` documents protected governance collections and effective deletion windows; legal hold blocks metadata deletion and audits `privacy.retention_legal_hold`. Postgres mode exposes tenant-scoped transactional retention enforcement + dry-run reporting through `runtime.services.retention`; Postgres tenant privacy patches trigger the enforcement path; operator CLI `scripts/postgres-retention-runner.mjs` runs preview/enforce for explicit tenant lists with `--dry-run`/`--out` and external scheduling. **External:** staging scheduled run evidence, regional residency, vault/KMS, governance deletion. |
| [x] | SEC-005 | Implement audit evidence retention | [Privacy & Data](docs/security/03-privacy-and-data-retention.md) | Policy snapshot, `evidence_retention` fields, and legal-hold gate landed in dev validation; Postgres retention enforcement preserves governance/audit and high-scale collections by design while purging metadata-only collections under transactional legal-hold controls; Postgres retention operator runner provides metadata-only dry-run/enforce summaries for explicit tenant scope. **External:** immutable production storage, durable legal evidence archive/export signoff, staging scheduled retention run evidence, and governance deletion policy per release checklist. |
| [x] | SEC-006 | Implement platform observability | [Observability](docs/backend/08-observability.md) | `/metrics`, `/v1/observability`, core counters, and minimum alert/dashboard contracts are documented and smoke-tested. `scripts/observability-slo-evidence.mjs` / `npm run observability:slo:evidence` validates metadata-only scrape auth, dashboards, alert routes, SLO targets, incident drill, on-call owner, and redaction policy evidence. **External:** authenticated production scraping, dashboard/alert deployment, incident drill execution, and SLO/on-call signoff. |
| [x] | SEC-007 | Implement disaster recovery basics | [Backend Architecture](docs/backend/01-backend-architecture.md) | Developer-validation: `scripts/backup-dev-store.mjs` / `scripts/restore-dev-store.mjs` (JSON store backup + manifest sha256/bytes, traversal/checksum/byte guards, `--dry-run`, `--yes` to write), `npm run dr:backup:dev` / `dr:restore:dev`, `docs/disaster-recovery.md` + operator runbook; `scripts/dr-restore-evidence.mjs` / `npm run dr:restore:evidence` validates production restore/failover drill metadata; `tests/unit/dr-backup-restore.test.mjs`. Postgres runtime persistence exists for migrated route families, including the production release evidence ledger. **External:** scheduled encrypted Postgres backups, restore/failover drill execution, RPO/RTO signoff, immutable evidence recovery, and staging DB-backed recovery evidence. |
| [x] | SEC-008 | Implement evidence chain-of-custody | [Enterprise Gap Backlog](docs/product/08-enterprise-production-gap-backlog.md) | Developer-validation: `src/lib/custody.mjs` (`json-key-sorted-v1` hardened canonicalization, SHA-256 digest manifests, `verifyCustodyManifest`), report/finding exports attach `custody` + embedded Custody sections (markdown/html), `/v1/custody/verify` verifies payload/custody pairs without payload echo and audits `custody.verified`, custody includes `previous_audit_hash` and optional `previous_tenant_audit_hash`, export/verification audits record safe digest metadata only, and Postgres report/finding exports are wired through `runtime.services.reports` (`tests/unit/custody.test.mjs`, `tests/unit/audit-chain.test.mjs`, `tests/unit/postgres-service-adapters.test.mjs`, `tests/integration/completion-polish.test.mjs`, `tests/integration/custody-verification-api.test.mjs`). `scripts/evidence-snapshot-manifest.mjs` / `npm run evidence:snapshot:manifest` validates immutable snapshot batch metadata and rejects raw payloads/logs/tokens/secrets/ciphertext. **External:** durable immutable evidence snapshots, signing key/KMS ceremony, retained export custody storage, retention/legal-hold enforcement evidence, staging signoff. |
| [x] | SEC-009 | Build compliance report mappings | [Enterprise Gap Backlog](docs/product/08-enterprise-production-gap-backlog.md), [Compliance](docs/security/04-compliance-and-references.md) | Developer-validation: `src/contracts/complianceReports.mjs` (templates for executive, board, technical, soc, audit, soc2, iso27001, dora, nis2, internal_audit), `buildComplianceMapping` / export sections wired in dev `reports.mjs` and Postgres `reportServiceAdapters.mjs`, `summary.compliance` on create, `tests/unit/compliance-reports.test.mjs`. **External:** auditor/legal sign-off, staging export review with customer compliance teams, alignment with immutable evidence retention (SEC-005/SEC-008). |

## 7. QA and release

| Status | ID | Task | Implementation doc | Completion goal |
|---|---|---|---|---|
| [x] | QA-001 | Build unit test suite | [Test Strategy](docs/backend/05-test-strategy.md) | Critical services and detection logic have unit tests. |
| [x] | QA-002 | Build integration test environment | [Test Strategy](docs/backend/05-test-strategy.md) | Simulated probe/agent flow works in CI. |
| [x] | QA-003 | Build agent install test matrix | [Agent Installation](docs/agent/02-installation-and-packaging.md) | `tests/unit/agent-package.test.mjs` covers generic tarball builder/manifest/signature; `tests/unit/agent-runtime.test.mjs` covers update-manifest verifier CLI; install path tests exist in CI. `tests/unit/agent-install-matrix-evidence.test.mjs` validates the metadata-only evidence contract for Ubuntu/Debian/RHEL/Amazon Linux/container/Kubernetes rows. **External:** execute real Ubuntu, Debian, RHEL-family, Amazon Linux, Docker, Kubernetes distro matrix. |
| [x] | QA-004 | Build E2E validation scenario | [Release Plan](docs/product/06-release-plan.md) | Local + hosted production-like E2E matrix complete: `scripts/local-staging-e2e-matrix.mjs` / `npm run staging:local:e2e-matrix` and `scripts/hosted-staging-e2e-matrix.mjs` / `npm run staging:hosted:e2e-matrix` execute OIDC login (local JWKS fixture or bundled hosted staging OIDC), signed agent registration, signed probe-worker poll (`ASTRANULL_PROBE_MODE=signed-worker`), safe validation loop, verdict explanation, report export custody, and SOC high-scale governance drill (hosted uses adapter-disabled start gate). Writes contract-valid `staging_e2e_matrix` with `overall_status=passed` (`tests/unit/local-staging-e2e-matrix.test.mjs`, `tests/unit/hosted-staging-stack.test.mjs`). Verified 2026-07-03 on Docker Compose (`npm run staging:local:attest`) and Railway hosted staging (`npm run staging:hosted:attest` against `https://control-plane-production-3404.up.railway.app`). **Deferred (operational config):** Playwright browser/accessibility matrix against customer domain. |
| [x] | QA-005 | Build security regression tests | [Security Model](docs/security/01-security-model.md) | RBAC, export redaction, packet reject, observability, safety-check, service-layer API rate-limit regression, notifications rule validation and delivery-ledger coverage (`tests/unit/notifications.test.mjs`, integration alpha platform), legal-hold retention regression, evidence-gated agent observation (`tests/unit/agent-runtime.test.mjs`), SOC post-test report RBAC/close gate and high-scale metadata telemetry (`tests/integration/hardening.test.mjs`), placement diagnostics/readiness (`tests/unit/readiness.test.mjs`), agent package signing (`tests/unit/agent-package.test.mjs`), signed agent update releases, tenant trust-key pinning, agent poll/status, and Postgres agent-update repository/service wiring (`tests/unit/agent-updates.test.mjs`, `tests/unit/postgres-agent-update-repository.test.mjs`, `tests/integration/agent-updates-api.test.mjs`), Postgres startup preflight (`tests/unit/postgres-startup-check.test.mjs`), Postgres `coreCatalogRepository` (incl. `patchCurrentTenant`) / `auditRepository` (`appendAuditEvent`) / `authTokenRepository` / `agentControlRepository` / `validationEvidenceRepository` / `reportRepository` primitives (`tests/unit/postgres-core-catalog-repository.test.mjs`, `tests/unit/postgres-audit-repository.test.mjs`, `tests/unit/postgres-auth-token-repository.test.mjs`, `tests/unit/postgres-agent-control-repository.test.mjs`, `tests/unit/postgres-validation-evidence-repository.test.mjs`, `tests/unit/postgres-report-repository.test.mjs`), addressed `agc_v1` agent credentials and `agent.auth_denied` audit boundaries (`tests/unit/addressed-secrets.test.mjs`, `tests/unit/agent-auth.test.mjs`, `tests/integration/auth-boundary.test.mjs`), signed probe `probe_profile`/port vectors, worker bounded-metadata routing, SOC Console UI smoke in CI, secret vault API/RBAC/scope (`tests/unit/secret-handling.test.mjs`, `tests/integration/secret-vault-api.test.mjs`), addressed bootstrap/service-account secrets and parser rejection (`tests/unit/addressed-secrets.test.mjs`, `tests/unit/tokens.test.mjs`), service-account create/list/revoke/rotate, invalid addressed bearer audit without secret material, and opaque garbage no-op (`tests/unit/service-accounts.test.mjs`, `tests/integration/auth-boundary.test.mjs`), developer-validation DR backup/restore (`tests/unit/dr-backup-restore.test.mjs`), visualization smoke (`tests/e2e/ui-smoke.test.mjs`), OIDC JWT auth config and API boundary (`tests/unit/auth-config.test.mjs`, `tests/integration/oidc-auth-api.test.mjs`), plus metadata-only evidence validators for OIDC preflight, edge protection, agent SBOM/provenance, agent install matrix, governed adapter, provider approval, kill-switch drill, Postgres concurrency, DR restore, rollback/fix-forward plan, UI accessibility matrix, notification retry/provider config, probe fleet matrix, vector safety policy, secret rotation, observability SLO, support readiness, immutable evidence snapshots, and Postgres tenant query audit. Gated Postgres acceptance for staging only. Open: load/abuse, concurrent tenant isolation execution, staging signed-worker fleet matrix. |
| [x] | QA-006 | Maintain production readiness checklist | [Release Plan](docs/product/06-release-plan.md), [Enterprise Gap Backlog](docs/product/08-enterprise-production-gap-backlog.md) | [`docs/release-checklist.md`](docs/release-checklist.md) updated: **Local evidence** and **Hosted staging evidence** (internal SOC metadata on Railway Postgres + bundled OIDC) plus **Deferred (operational config)** (enterprise IdP/KMS/provider credentials). Evidence inventory verified with 31/31 accepted evidence kinds (`environment=local-staging` and `environment=staging`); `scripts/production-readiness-gap-audit.mjs` parses checklist and release-plan promotion gate tables and reports `production_ready=true` when inventory and documented gates are closed — that scorecard does not prove customer-specific production launch or per-tenant operational wiring. Operator evidence validators cover all 31 contract kinds. **Deferred (operational config):** enterprise IdP tenant mapping, real notification provider credentials, hosted KMS/HSM, customer cloud/CDN integrations. |
| [x] | QA-007 | Control-plane release packaging | [Operator runbook](docs/operator-local-runbook.md) | Root `Dockerfile`, `.dockerignore`, Node-only `make verify`, GitHub CI workflow, Postgres migration runner, startup preflight, and production Postgres startup boundary exist; `scripts/rollback-fixforward-evidence.mjs` / `npm run rollback:evidence` plus production release evidence kind `rollback_fixforward` validate metadata-only rollback/fix-forward plans (`tests/unit/rollback-fixforward-evidence.test.mjs`). **External:** image build/sign/promotion, staging run evidence, runtime secret custody, and executed rollback/forward-fix tabletop or drill signoff. |

## 8. Optional WAF posture add-on

These rows track the imported WAF posture add-on. They are **not** part of the default core product path until feature flags are enabled. Do not make WAF/cloud credentials, automatic discovery, or WAF rule deployment mandatory.

| Status | ID | Task | Implementation doc | Completion goal |
|---|---|---|---|---|
| [x] | WAF-001 | Define WAF posture product scope and flags | [WAF Posture Management](docs/product/09-waf-posture-management.md), [WAF Feed Order](docs/agent-prompts/00-waf-agent-feed-order.md) | `ASTRANULL_WAF_POSTURE_ENABLED` and `ASTRANULL_EXTERNAL_DISCOVERY_ENABLED` default off; `loadRuntimeConfig().featureFlags` exposes both; `/v1/waf/*` returns `waf_feature_disabled` when off; WAF UI disabled state preserves no-access-first/no-credential behavior. Verified by `tests/unit/auth-config.test.mjs`, `tests/integration/waf-posture-api.test.mjs`, and `tests/e2e/ui-smoke.test.mjs`. |
| [x] | WAF-002 | Add safe WAF validation policy | [Safe WAF Validation Policy](docs/security/05-waf-safe-validation-policy.md) | `src/contracts/wafPosture.mjs` rejects forbidden raw evidence keys, unsafe WAF validation profiles, SOC-gated/prohibited customer runs, and discovery auto-approval; `/v1/waf/validations` stores metadata-only safe marker/rate-limit validation envelopes. Signed WAF probe profiles carry only allowlisted scenario metadata and remain tamper-sensitive. Finalize rejects naked `validation_passed=true` without a bound safe test run or explicit metadata-only scenario evidence. **External:** staging security review and full WAF add-on release evidence. |
| [x] | WAF-003 | Design WAF data model and API | [WAF Data Model](docs/backend/12-waf-posture-data-model.md), [WAF API Contract](docs/backend/13-waf-posture-api-contract.md) | `db/migrations/0008_waf_posture.sql` / `db/schema.sql` add tenant-scoped WAF assets, validation runs, posture snapshots, baselines, drift, optional connectors, CVE pipeline, and recommendations with forced RLS and composite FKs; dev-json and Postgres `/v1/waf/assets`, `/v1/waf/coverage`, `/v1/waf/validations`, `/v1/waf/drift-events`, and `/v1/waf/reports/:kind/export` (metadata-only `{ payload, custody }` via `buildCustodyManifest`) are feature-gated and RBAC-protected through `runtime.services.wafPosture`. Dev-json WAF orchestrator routes are wired at `/v1/waf/validation-plans` (list/create/scheduled/execute/cancel), `/v1/waf/baselines/:id/approve`, `/v1/waf/drift-events/:id/retest`, `GET /v1/waf/retests` (optional `drift_event_id` filter), `/v1/waf/retests/:id/execute`, and Postgres `/v1/waf/retests/:id/complete`. `db/migrations/0011_waf_orchestrator.sql` adds Postgres tables for `waf_validation_plans`, `waf_baseline_approvals`, and `waf_retest_requests` plus `waf_baselines.updated_at`; `wafOrchestratorRepository` / `wafOrchestratorServiceAdapters` expose `runtime.repositories.wafOrchestrator` and `runtime.services.wafOrchestrator` via `createPostgresRuntime()`. Postgres routes fail closed with `postgres_waf_orchestrator_unavailable` when the service is not injected. Postgres plan/retest execute use safe continuation: assets×scenarios queue capped by `max_concurrent`; at most one new `runtime.services.testRuns.startTestRun` per tick (signed-worker active-run gate); resumes from `delegated_jobs` with `continuation_required` while `running`; plan `completed` means all jobs delegated (not posture closure). Retest `delegated` means all scenarios delegated; `completeRetest` on `POST /v1/waf/retests/:id/complete` closes from finalized test-run verdict evidence via `getTestRun` only (bound to `delegated_jobs` metadata); `completeRetestWithDriftAndAudit` atomically persists retest status/verdict, linked drift status, and `waf.retest.completed` in one tenant transaction. Preflight `waf_orchestration_batch_too_large` when queue exceeds `max_concurrent` (no side effects); audits `waf.validation_plan.executed` / `waf.retest.delegated` on new delegation. Fail-closes on missing service/`startTestRun`/`getTestRun`, non-`signed-worker` mode, batch overflow, missing asset/target, invalid safe check, or delegation persistence failure (`waf_retest_already_delegated` when fully delegated, `waf_retest_already_completed`). Postgres plan/retest execute use developer-validation-hardened DB execution leases coordinate concurrent API/runner execute ticks (`execution_lock_token`, `execution_lock_expires_at`; claim-before-`startTestRun`; claim/finish lifecycle-gate plan states and retest statuses; claim miss re-read maps lifecycle errors; TTL at least safe-run `timeout_ms` + 30s buffer or larger configured lease; validation-plan cancel clears active leases and best-effort cancels delegated safe runs in `delegated_jobs`; `409 waf_orchestrator_execution_in_progress` only for true lease conflicts; stale-finish/null-finish compensating cancel with best-effort lease release; finish throw after `startTestRun` also best-effort releases the lease—cannot resurrect `cancelled`; hard process crash after `startTestRun` before `delegated_jobs` persistence remains crash-recovery/staging evidence open—no production at-most-once delegation claim). **External:** staging runner evidence for `npm run waf:orchestrator:runner`; live/staging DB acceptance; provider connector workers; security/observability/release signoff; remaining contract route families where not wired. |
| [x] | WAF-004 | Build WAF fingerprinting and coverage | [WAF Fingerprinting and Coverage](docs/detection/13-waf-fingerprinting-coverage.md) | Dev-json coverage aggregation classifies protected/underprotected/unprotected/unknown/excluded from current metadata snapshots and never marks protected without validation pass. Safe check catalog now includes `waf.fingerprint.safe`, `waf.marker_rule.safe`, `waf.origin_bypass.safe`, and `waf.low_rate_limit.safe` with signed WAF scenario metadata. **External:** actual fingerprint signal collection and connector reconciliation. |
| [x] | WAF-005 | Build WAF effectiveness and drift checks | [WAF Effectiveness and Drift](docs/detection/14-waf-effectiveness-drift.md), [WAF Agent/Probe Updates](docs/agent/08-waf-agent-probe-updates.md) | Agent runtime now advertises WAF observer capabilities and uploads sanitized WAF canary metadata (`observation_type`, `route_label`, `placement_confidence`, `direct_origin`, `protected_path`, `log_pointer_hash`) while rejecting raw fields; signed WAF marker probe jobs include tamper-sensitive scenario metadata. Dev-json WAF validations can bind to an existing safe test run and derive protected/underprotected/unknown posture from metadata-only probe and nonce-matched agent events. Underprotected/unprotected posture now creates or refreshes metadata-only findings, and protected-to-weakened posture creates or refreshes open drift events for marker failures, origin bypass, mode/fingerprint loss with RBAC patch workflow. Postgres orchestrator persistence covers validation plans, baseline approvals, and retest requests (`0011`); plan/retest execute use safe continuation (one new signed-worker job per tick); retest `complete` atomically persists retest/drift/`waf.retest.completed` via `completeRetestWithDriftAndAudit` from `getTestRun` verdict evidence bound to `delegated_jobs` metadata in developer validation. **External:** staging scheduling/execution evidence for `npm run waf:orchestrator:runner`; staging retest-to-drift evidence; Postgres live-event correlation parity, connector config drift workers, scheduled drift compare, remediation exports. Plan/retest execute developer-validation-hardened DB execution leases coordinate concurrent API/runner execute ticks (`execution_lock_token`, `execution_lock_expires_at`; claim-before-`startTestRun`; claim/finish lifecycle-gate plan states and retest statuses; claim miss re-read maps lifecycle errors; TTL at least safe-run `timeout_ms` + 30s buffer or larger configured lease; validation-plan cancel clears active leases and best-effort cancels delegated safe runs in `delegated_jobs`; `409 waf_orchestrator_execution_in_progress` only for true lease conflicts; stale-finish/null-finish compensating cancel with best-effort lease release; finish throw after `startTestRun` also best-effort releases the lease—cannot resurrect `cancelled`; hard process crash after `startTestRun` before `delegated_jobs` persistence remains crash-recovery/staging evidence open—no production at-most-once delegation claim) implemented locally. |
| [x] | WAF-006 | Add optional WAF/CDN/cloud connectors | [WAF/CDN/Cloud Connectors](docs/integrations/01-waf-cdn-cloud-connectors.md) | Optional connector framework now supports feature-gated `/v1/connectors` create/list/validate/poll/disable and snapshot-list routes in dev-json and Postgres. The first slice stores only allowlisted metadata config/summaries, rejects nested raw/secret fields, keeps credentials as `secret_id` pointers, audits lifecycle metadata, and performs no outbound provider calls or automatic discovery. **External:** real read-only Cloudflare/AWS/provider workers, vault-backed credential use, retry/backoff health, connector-to-asset reconciliation, config drift, UI, and staging provider evidence. |
| [x] | WAF-007 | Add remediation/SIEM/SOAR workflows | [Remediation/SIEM/SOAR Connectors](docs/integrations/02-remediation-siem-soar-connectors.md), [WAF Remediation Ticket Template](docs/templates/waf-remediation-ticket-template.md) | Action item model (`createActionItem`, `validateActionItem`, `groupFindings`) in `src/contracts/wafPosture.mjs`; SIEM event payload builder (`buildSiemEventPayload`) producing `astranull.waf_event.v1` schema; `listActionItems`, `createActionItemFromFinding`, `patchActionItemStatus`, `buildRemediationPayload`, `deliverActionItem` service methods with deduplicated action items; safe-by-default outbound delivery via `src/lib/remediationDelivery.mjs` (HTTPS-only, bounded timeout/payload, redacted bodies, metadata-only default); Jira/ServiceNow/Slack/webhook/SIEM opt-in delivery behind `ASTRANULL_REMEDIATION_DELIVERY_MODE` + per-channel URLs; API routes wired at `/v1/waf/action-items` and `POST /v1/waf/action-items/:id/deliver` (dev-json and Postgres repository/service/runtime parity, async route handling); Postgres `waf_asset_id` dedupe via `db/migrations/0010_waf_addon_postgres_parity.sql`; unit/integration tests for delivery adapters. **External:** staging delivery evidence, vault-backed credentials, bidirectional ticket status sync. |
| [x] | WAF-008 | Add CVE-to-WAF mitigation pipeline | [Live Exposure Defense CVE Pipeline](docs/detection/15-live-exposure-defense-cve-pipeline.md), [WAF Rule Recommendation Template](docs/templates/waf-rule-recommendation-template.md) | `src/contracts/cvePipeline.mjs` (558 lines) and `src/services/cvePipeline.mjs`: 8-stage pipeline (ingest→triage→match→validate→recommend→ticket→retest→resolved); triage scoring with known-exploited/internet-facing/critical-asset weighting; asset matching with 6 confidence levels; vendor-specific WAF rule recommendation templates for cloudflare/akamai/aws/azure/gcp/imperva/fortinet/generic; forbidden exploit field rejection; feature-gated API routes at `/v1/waf/cve-pipeline` plus metadata-only feed ingest at `POST /v1/waf/cve-pipeline/ingest` (`src/lib/cveFeedIngest.mjs`, `ingestCveFeed`, operator CLI `npm run cve:feed:ingest`); dev-json and Postgres repository/service/runtime parity; unit/integration tests pass. **External:** safe validation executor, staging evidence. |
| [x] | WAF-009 | Add approval-gated external discovery | [Enhanced External Discovery](docs/product/10-enhanced-external-discovery.md) | `src/contracts/externalDiscovery.mjs` (394 lines) and `src/services/externalDiscovery.mjs` (423 lines): entity model (6 types), candidate lifecycle state machine (8 states), discovery modes D0–D4, confidence scoring from 7 source types, approval/rejection workflows with audit events, discovery inbox filtering, unapproved-candidate testing guard; feature-gated API routes at `/v1/discovery/*` (dev-json and Postgres repository/service/runtime parity; Postgres entity create persists `id === entity_id` for stable candidate FK linkage); D0 declared-only mode regression-safe; 10 unit tests pass. **External:** DNS/CT/connector data source integrations, staging evidence. |
| [x] | WAF-010 | Add active protection/digital supply chain design | [Active Protection and Digital Supply Chain](docs/product/11-active-protection-and-digital-supply-chain.md) | `src/contracts/supplyChainRisk.mjs` (292 lines) and `src/services/supplyChainRisk.mjs` (456 lines): AP0 detect-only + AP1 ticket workflow; 6 exposure types (dangling CNAME, deleted cloud app, dangling script, orphaned redirect, vendor dependency, subdomain takeover); risk severity scoring; state machine (suspected→confirmed→remediation_pending→resolved→accepted_risk→customer_custody); metadata-only dangling CNAME and dependency assessment (no DNS lookups or scraping); redacted remediation ticket generation; deduplication by hostname+exposure_type; remediation ticket `retest_link` uses `/v1/waf/supply-chain/risks?risk_id=<id>`; no automated acquisition code paths; feature-gated API routes at `/v1/waf/supply-chain/*` (dev-json and Postgres repository/service/runtime parity); 11 unit tests pass. **External:** actual data source integrations, legal gate formalization, staging evidence. |
| [x] | WAF-011 | Build WAF posture UX | [WAF Posture UX](docs/ux/13-waf-posture-ux.md), [WAF Workflows](docs/flows/07-waf-posture-workflows.md) | `#waf-posture` operational console shows feature-disabled state, coverage cards, declared assets, validation runs, metadata-only evidence safety, and safe marker actions. **Developer-validation UI coverage:** drift events queue on `#waf-posture` lists/renders drift with metadata-only before/after summaries (no filter controls), drift status patch only (no note input in UI), retest request / execute / complete controls wired to `/v1/waf/drift-events` and `/v1/waf/retests`, and retest id/status hydrated from `GET /v1/waf/retests` (session override after in-session actions; orchestrator fail-closed errors surfaced in UI). **Reports panel** on `#waf-posture` exports metadata-only WAF reports (`GET /v1/waf/reports/:kind/export`) with summary-only custody manifest preview (no full payload dump). **Validation-plan operator panel** on `#waf-posture` lists all/scheduled plans via `/v1/waf/validation-plans` (+ `/scheduled`), exposes create/execute/cancel for safe plans, surfaces metadata-only `delegated_jobs` / `continuation_required` on execute, treats plan `completed` as all jobs delegated (not posture closure), and degrades gracefully when `postgres_waf_orchestrator_unavailable`. New UI pages added: CVE Pipeline (`renderCvePipelinePage` with stage badges, triage/match/recommend detail panel), Discovery Inbox (`renderDiscoveryPage` with inbox/all-candidates tabs, entity management, D0–D4 mode selector), Supply Chain Risks (`renderSupplyChainPage` with exposure badges, assessment forms), Remediation Action Items (`renderRemediationPage` with status management, SIEM preview, grouping toggle); confidence bars, stage/severity/state badge CSS, discovery mode selector styles. E2E/ui-helpers tests pass. WAF add-on UX is **not production-ready**. **External:** immutable report storage/signing; accessibility/browser matrix evidence for the full WAF add-on; staging/live DB acceptance; staging scheduling/execution evidence for `npm run waf:orchestrator:runner`; provider connector workers and connector config drift workers; security/observability/release signoff per [`docs/release-checklist.md`](docs/release-checklist.md). |
| [x] | WAF-012 | WAF QA and release evidence controls | [WAF Build Backlog](docs/progress-waf-posture-backlog.md), [Exposure Management Research Map](docs/sources/exposure-management-research-map.md) | Added unit/integration/e2e/schema/security coverage for WAF flags, RBAC, contracts, DB RLS/migrations, dev-json API, Postgres WAF repository/service/runtime wiring (including `0011_waf_orchestrator` and `tests/unit/postgres-waf-orchestrator-repository.test.mjs`), UI smoke, signed probe profiles, agent observation metadata, live metadata-only correlation, protected-finalize evidence gates, WAF posture findings, first behavior-drift events, and metadata-only connector config/snapshot APIs. Postgres validation-plan and retest execute record `delegated_jobs` (delegation-only); retest `complete` closes from test-run verdict evidence bound to `delegated_jobs` metadata; `completeRetestWithDriftAndAudit` atomically persists retest status/verdict, linked drift status, and `waf.retest.completed`—not final WAF posture closure by itself. Latest targeted local checks passed (272 focused tests): Postgres WAF orchestrator adapters/runtime/repository/migrations/server wiring, probe coordinator, and WAF posture API; `node scripts/lint.mjs`, `node scripts/safety-check.mjs`, `node scripts/validate-db-schema.mjs`, tenant-query audit, and `git diff --check`. WAF add-on Postgres route parity, migration `0010_waf_addon_postgres_parity`, lint, and safety-check. **External:** staging scheduling/execution evidence for `npm run waf:orchestrator:runner`; Postgres live-event correlation parity, real provider connector workers, connector config drift, CVE/discovery/remediation live evidence, reports/custody, live/staging DB acceptance, live deployment/observability/security/release signoff. Plan/retest execute developer-validation-hardened DB execution leases coordinate concurrent API/runner execute ticks (`execution_lock_token`, `execution_lock_expires_at`; claim-before-`startTestRun`; claim/finish lifecycle-gate plan states and retest statuses; claim miss re-read maps lifecycle errors; TTL at least safe-run `timeout_ms` + 30s buffer or larger configured lease; validation-plan cancel clears active leases and best-effort cancels delegated safe runs in `delegated_jobs`; `409 waf_orchestrator_execution_in_progress` only for true lease conflicts; stale-finish/null-finish compensating cancel with best-effort lease release; finish throw after `startTestRun` also best-effort releases the lease—cannot resurrect `cancelled`; hard process crash after `startTestRun` before `delegated_jobs` persistence remains crash-recovery/staging evidence open—no production at-most-once delegation claim) implemented locally. WAF add-on is not production-ready. |
| [x] | WAF-013 | Implement WAF risk scoring and deployment tiers | [WAF Risk and Coverage Analytics](docs/backend/14-waf-risk-coverage-analytics.md) | `wafRiskService` computes 0–100 scores with factor-level evidence; assigns `tier_1`–`tier_4`; persists on posture snapshots; feeds roadmap API. |
| [x] | WAF-014 | Implement coverage analytics APIs | [WAF Risk and Coverage Analytics](docs/backend/14-waf-risk-coverage-analytics.md), [WAF API Contract](docs/backend/13-waf-posture-api-contract.md) | Routes wired for `/v1/waf/coverage` trend/ratio, `/vendors`, `/entities`, `/criticality`, `/geography`, `/risk-roadmap`, `/vendor-consolidation`; OpenAPI artifact updated; integration tests. |
| [x] | WAF-015 | Build executive WAF analytics UX | [WAF Posture UX](docs/ux/13-waf-posture-ux.md) | `#waf-posture` Overview tab shows vendor mix and geography cards from `/v1/waf/coverage/vendors` and `/geography`; Roadmap tab renders Tier 1–4 items from `/v1/waf/coverage/risk-roadmap` with factor breakdown; Assets tab table includes per-asset pass rate and connector rule health columns; asset detail effectiveness section wired to `GET /v1/waf/assets/:id` effectiveness fields. UI smoke and ui-helpers unit tests updated. |
| [x] | WAF-016 | Add WAF compliance audit export | [WAF Compliance Audit Evidence](docs/product/12-waf-compliance-audit-evidence.md) | Backend: `compliance_audit` in `WAF_REPORT_KINDS` and `GET /v1/waf/reports/compliance_audit/export` (dev-json + Postgres `exportWafReport`), metadata-only payload with executive coverage summary, scope declaration, asset sample, validation methodology, drift/exception summary, connector attestation, CVE exposure summary, validation pass rates, entity/geography rollups, `control_mapping_appendix` (static framework themes + live metrics + artifact id refs), and `exception_register`. Dev-json sources exceptions from `wafExceptions`; Postgres exports populate in-scope CVE exposure from the injected `cvePipeline` repository and active approved exceptions from durable `waf_exceptions` persistence (`0022_waf_exceptions.sql`) via `POST /v1/waf/assets/:id/exception` and `GET /v1/waf/exceptions`. Custody via `buildCustodyManifest`; WAF report picker exposes `compliance_audit`; OpenAPI enum updated; `tests/integration/waf-posture-api.test.mjs`, `tests/unit/postgres-service-adapters.test.mjs`, `tests/unit/postgres-waf-posture-repository.test.mjs`, `tests/unit/ui-helpers.test.mjs`, `tests/e2e/ui-smoke.test.mjs`. **External:** immutable storage/signing and staging export signoff. |
| [x] | WAF-017 | Expand scenario cadence and vendor catalog | [WAF Scenario Cadence](docs/detection/16-waf-scenario-cadence.md), [WAF Fingerprinting](docs/detection/13-waf-fingerprinting-coverage.md) | `CONTROL_BYPASS_CLASSES`, `deriveControlBypassStatus`, and `normalizeScenarioIntakeInput` in `src/contracts/wafPosture.mjs`; 52-entry metadata-only catalog in `db/seeds/waf-product-catalog.json` with `seedWafProductsIfEmpty` / classifier helpers in `src/lib/wafProductCatalog.mjs`; `GET /v1/waf/products`, `GET|POST /v1/waf/scenario-intake` (dev-json + Postgres adapter parity); durable `waf_scenario_intakes` table (`0019` + composite `UNIQUE (tenant_id, id)` in `0020`); asset detail `effectiveness.control_bypass_*`; `#waf-posture` scenario cadence panel with intake form; validate/seed CLIs are covered by WAF-021; unit/integration/e2e tests pass. |
| [x] | WAF-018 | Add criticality coverage rollup | [WAF Risk and Coverage Analytics](docs/backend/14-waf-risk-coverage-analytics.md), [WAF API Contract](docs/backend/13-waf-posture-api-contract.md) | `buildCriticalityRollup` in `wafCoverageService`; `GET /v1/waf/coverage/criticality` (dev-json + Postgres `getWafCoverageCriticality`); `criticality_rollup` in `executive_coverage` and `compliance_audit` exports (`wafReports.mjs`); overview criticality card via `renderWafCriticalityCard` (`ui-helpers.js`) on `#waf-posture` (`app.js`); OpenAPI path/schemas + `validate-waf-openapi` required path; `tests/unit/waf-coverage-service.test.mjs`, `tests/integration/waf-posture-api.test.mjs`, `tests/unit/ui-helpers.test.mjs`, `tests/e2e/ui-smoke.test.mjs`. **External:** staging export signoff, immutable storage/signing. |
| [x] | WAF-019 | Analytics schema extensions | [Analytics Schema Extensions](docs/backend/16-waf-analytics-schema-extensions.md), [WAF Data Model](docs/backend/12-waf-posture-data-model.md) | `0017_waf_analytics_extensions.sql`: geography fields on `waf_assets`, `scenario_pass_rate`/`control_bypass_status` on snapshots, `waf_coverage_daily_rollups` with RLS; `db/schema.sql` parity; `block_page_expectation` scenario family and `region` entity type; Postgres repository upsert/list rollup methods + tests; `wafCoverageRollupWorker` + `npm run waf:coverage:rollup:runner` (cron-friendly metadata-only CLI); dev-json and Postgres `GET /v1/waf/coverage` now serve `trend[]` from persisted daily rollups when present and fall back to snapshot-derived trend otherwise. |
| [x] | WAF-020 | Multi-vendor CVE mitigation playbooks | [Multi-Vendor CVE Playbook](docs/detection/17-multi-vendor-cve-mitigation-playbook.md) | `buildCveMitigationPlaybook` / vendor slice grouping in `src/contracts/cvePipeline.mjs`; dev-json + Postgres playbook persistence (`cveMitigationPlaybooks`, `triage_summary_json.mitigation_playbook`); `getCveMitigationPlaybook`, `approveCveMitigationPlaybook`, `executeCoordinatedCveRetest` in `src/services/cvePipeline.mjs` with parent/child `cve_mitigation` action items; routes `GET/POST /v1/waf/cve-pipeline/:id/playbook`, `POST .../playbook/approve`, `POST .../coordinated-retest`; integration tests in `tests/integration/cve-pipeline-api.test.mjs`. **External:** staging security review, immutable ticket custody, live multi-vendor operator workflow. |
| [x] | WAF-021 | WAF product catalog seed pipeline | [Product Catalog Pipeline](docs/backend/15-waf-product-catalog-pipeline.md) | `db/seeds/waf-product-catalog.json` (52 entries, manifest checksum/version), `src/lib/wafProductCatalog.mjs` (validate/classify/regression fixtures), `scripts/validate-waf-product-catalog.mjs` / `scripts/seed-waf-product-catalog.mjs`, `npm run waf:catalog:validate` / `waf:catalog:seed` (dev-json + Postgres idempotent upsert), fingerprint probe evidence carries `waf_fingerprint_catalog_version` + entry count via `enrichProbeMetadataWithWafCatalog`, `tests/unit/waf-product-catalog.test.mjs`. |
| [x] | WAF-022 | Board roadmap brief and OpenAPI parity | [WAF Compliance Audit Evidence](docs/product/12-waf-compliance-audit-evidence.md), [WAF API Contract](docs/backend/13-waf-posture-api-contract.md) | `board_roadmap_brief` in `WAF_REPORT_KINDS` and `GET /v1/waf/reports/board_roadmap_brief/export` (dev-json + Postgres `exportWafReport`), metadata-only payload with coverage trend, Tier 1–2 summary counts/highlights, vendor mix, geography highlights, investment phases, procurement justification narrative, and roadmap API reference; WAF report picker exposes `board_roadmap_brief`; OpenAPI enum updated plus `/v1/waf/coverage/criticality` and CVE playbook routes documented; `npm run api:waf:openapi:check` passes; `tests/integration/waf-posture-api.test.mjs`, `tests/unit/ui-helpers.test.mjs`, `tests/e2e/ui-smoke.test.mjs`. **External:** immutable storage/signing and staging evidence. |

## Milestone completion view

| Milestone | Goal | Required tasks |
|---|---|---|
| M0: Public entry and staff management boundary | Landing, sign-up, and internal-only management separated | P0-006, UX-015, UX-016, BE-017, BE-018 |
| M1: Clickable product shell | UX and object model visible | P0-001 to P0-005, UX-001 to UX-006 |
| M2: Agent registration (production) | Signed agent installs and phones home outbound-only | BE-003, BE-004, BE-009, AG-001, AG-002, AG-008, AG-009 |
| M3: First validation loop | Outside probe + inside observation + verdict | BE-007 to BE-013, AG-010/011, DET-001/002/013 |
| M4: Production safe checks | Full safe check library | DET-001 to DET-011, SEC-003 |
| M5: SOC high-scale governance | Request, approval, execution, stop, report | SOC-001 to SOC-008, DET-012 |
| M6: Enterprise readiness | Reports, RBAC, audit, security, reliability | UX-007 to UX-012, SEC-001 to SEC-007, QA tasks |
| M7: WAF posture core (optional) | No-access WAF MVP through connectors/CVE/discovery | WAF-001 to WAF-012 |
| M8: WAF analytics and compliance (optional) | Risk scoring, rollups, roadmap, audit exports, catalog scale | WAF-013 to WAF-022 |

## Open Design React revamp validation

Authoritative detail: [`docs/feature-ui-coverage-matrix.md`](docs/feature-ui-coverage-matrix.md).

| Status | ID | Area | Current evidence | Remaining work |
|---|---|---|---|---|
| [x] | RUX-001 | Remove old per-page HTML shell | Current web tree only keeps `apps/web/index.html` and `apps/web/react/index.html`; old public/staff page HTML files are deleted. | Keep route aliases pointed at React host during future changes. |
| [x] | RUX-002 | Target Groups React functionality | Browser verified create group, add target, patch settings, archive; `/v1/target-groups` and nested target APIs are used; archived groups excluded from `/v1/state`/readiness/placement. | Add target edit/delete controls if full detail parity is required. |
| [x] | RUX-003 | Environments actual-data widgets | Browser verified cards derive from active target groups, runs, and findings; hardcoded Production/Staging/DR rows removed; full React route matrix covered desktop/tablet/mobile. | None for the current React slice. |
| [x] | RUX-004 | Integrations actual connector workflow | Browser verified feature flags, secret storage, connector create/validate/poll/snapshot/disable, redacted APIs, and mobile layout. | Real provider workers and staging provider evidence remain external. |
| [x] | RUX-005 | Settings/API key workflow | Browser verified bootstrap token and service account create/revoke/rotate flows with one-time secrets and redacted lists. | SSO/SAML, users/roles, retention, and secret vault UI remain pending. |
| [x] | RUX-006 | Test Policies actual workflow | Backend `/v1/test-policies` create/list/update/archive implemented and tested; React page now supports create, pause/resume, cadence patch, and archive through `/v1/test-policies` PATCH/DELETE; nested Checks section removed from the policy page layout. | None for the current React slice. |
| [x] | RUX-007 | Dashboard, onboarding, agents, checks, runs, findings, evidence | Dashboard readiness/heatmap from evidence APIs. Onboarding creates target groups/targets, bootstrap tokens, safe runs. Agents: revoke/detail with probe-endpoint status (`detail-pages.tsx`, `functional-surfaces.tsx`). Runs: start-safe-run/cancel/finalize. Findings: accept-risk/close PATCH. Checks filtering/policy binding on functional surfaces. Evidence custody verify/export panels. **[!]** staging agent install/revoke drills across customer OS families. |
| [x] | RUX-008 | Governance pages: high-scale, reports, notifications, audit, release evidence | Route-specific React pages with real API actions (notification rules/retries, audit filter, release attestation, SOC kill switch/lifecycle). Customer `/high-scale` request/artifact workflow; `/soc` role gating. **[!]** PDF export and immutable report storage (KMS/object-store custody). |
| [x] | RUX-009 | WAF/CVE/discovery/supply-chain/remediation React pages | React fetches real WAF/discovery APIs; create/triage/approve/import/deliver forms on `functional-surfaces.tsx` (`DeliverPanel`, `IngestForm`, CVE create, supply-chain risk create, discovery approve/reject, WAF exception/validation). WAF asset detail route resolves entity-specific API detail. **[!]** staging connector drift reconciliation at scale. |
| [x] | RUX-010 | Detail routes | `DetailRoutePage` replaces static `100/72/64` factors with entity-specific scores/timelines for target groups, agents, runs, WAF assets, discovery entities, and tenants; fetches `/v1/target-groups/:id`, `/v1/test-runs/:id`, and `/v1/waf/assets/:id` when available. | Add deep tabbed subviews (events, findings, remediation) per entity if full wireframe parity is required. |
| [x] | RUX-011 | Support, subscription, staff/admin/SOC React pages | Support/Subscription use `/v1/subscription/current`. Staff console: signup approve/reject, tenant activate/suspend, entitlement grant UI. `/soc`: governed high-scale queue and kill-switch for `soc` role. **[!]** per-tenant SSO/SAML and users/roles UI deferred — production auth is OIDC/env against customer IdP (delegated identity model). |
| [x] | RUX-012 | Full route browser matrix and actual-data audit | Semantic/widget matrix (78 views) and visual-polish matrix (135 states) at 10/10 with zero structural findings. Static/generic audit items for detail routes, onboarding, notifications, audit, release-evidence, and `/soc` addressed in React. **[!]** broader desktop/tablet/mobile functional matrix rerun with provisioned browser stack + seeded records for new workflows. |

## Deep tracker reference

For minor implementation items, use [`docs/progress-detailed.md`](docs/progress-detailed.md). It expands UX, backend, agent, vector, SOC, and safety work into smaller trackable tasks with links to exact implementation docs.

Key deep specs to read before implementation:

| Area | Deep spec |
|---|---|
| Platform/pages/flows | [`docs/product/07-end-to-end-platform-spec.md`](docs/product/07-end-to-end-platform-spec.md) |
| Wireframes/interactions | [`docs/ux/12-full-wireframes-and-interactions.md`](docs/ux/12-full-wireframes-and-interactions.md) |
| Backend/agent/detection | [`docs/backend/10-agent-probe-detection-blueprint.md`](docs/backend/10-agent-probe-detection-blueprint.md) |
| Vector matrix | [`docs/detection/12-vector-test-matrix.md`](docs/detection/12-vector-test-matrix.md) |
